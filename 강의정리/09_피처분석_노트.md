# 09 — 피처 분석 노트 🔥

> **역할**: 피처 아이디어를 내고, **쓰기 전에 데이터로 전제를 검증**하는 작업 노트.
> [08 Phase2 전략](08_Phase2_데이터_및_전략.md)이 "실험 결과 대장"이라면 여기는 그 **앞단**이다.
> 08에는 모델을 돌려본 결과가 들어가고, 여기에는 **모델 돌리기 전에 확인한 데이터 사실**이 들어간다.

**중요도**: 🔥 검증된 사실 / ⭐ 유망 후보 / 📎 미검증 / ❌ 기각

---

## 0. 이 노트의 운영 규칙

1. **아이디어를 내면 먼저 전제를 pandas로 확인한다.** 모델부터 돌리지 않는다.
   - 근거: §2-A는 "합이 1일 것"이라는 그럴듯한 전제가 **틀린 채로** 제안될 뻔했다.
     확인 비용 1분, 안 했으면 학습 1런 + 잘못된 결론.
2. 🔴 **산포를 신호로 주장하기 전에 두 가지를 반드시 할 것** (2026-08-12 신설, 3회 위반 후)
   - **(a) 노이즈 바닥과 비교한다.** 관측된 sd가 개별 추정의 se보다 작으면 신호가 아니다.
   - **(b) 목적함수(Brier) 기여로 환산한다.** η²·상관·격차가 크다는 건 BSS 이득이 아니다.

   | 위반 | 내가 본 것 | 제대로 재니 |
   |---|---|---|
   | §1-F | fastball η²이 타깃의 **141배** | 타깃 위에 얹으면 **+0.6** (§3-E 기각) |
   | §1-K | `mu` 이동 **−0.008**(추정) | **−0.0013** (배포 계수로 실측) |
   | §2-C | 카운트별 드리프트 sd 0.0061 → **+14** | **0 ~ 3** (I²=4%, p=0.41) |

   세 번 다 **1/10 이하**였다. 전부 "그럴듯한 중간 지표"를 최종 지표로 착각한 것이다.
3. 🔴 **채점 범위를 기본값으로 정하지 말 것 — 순위가 뒤집힌다** (2026-08-12 신설)
   `R만` 채점은 fold 2021·2022의 F 라벨 아티팩트를 피하려는 **임시방편**이지 기본값이 아니다.
   **최종 채점은 전체다**(test에 F가 11.8% 있다). 판단 기준은 채점 방식이 아니라 **목표 연도**:
   ```
   목표 2021·2022  →  F가 아티팩트 시대(.70 vs .50). 전체 채점은 오염 → 쓰지 말거나 R만
   목표 2023·2024  →  F가 현대 체제(2024: .459 vs .4897). 전체 채점이 정당하고 이게 2025 대응
   ```
   실증(§3-I): 같은 데이터를 `R만`으로 줄 세우니 O1이 6/6 1위였는데, **전체·목표2024**로 바꾸니
   O1이 탈락하고 O2a가 1위가 됐다. **offset 이득이 F 행에서 거의 2배**(+46.6 vs +26.2)라
   R만 채점이 체계적으로 과소평가하고 있었다.
4. 🔴 **평균/집계에 넣을 관측은 배포 조건과 같은 것만** (2026-08-12 신설, 메인 세션 반박으로)
   전이 개수를 늘려 채택기준 5항의 **문구는 지켰는데 취지는 어겼다.**
   배포는 **최신 연도에서 적합 → 다음 연도에 적용**이다. 따라서:
   ```
   유효 전이 = 인접 전이(S = T−1)만.  2021→22 · 2022→23 · 2023→24
   ```
   실증(§2-E): `O2a` 12전이 평균 **+32.9** vs 중앙 **+1.8**. 이득이 전부 **2021 출처**에서 나왔는데
   **배포는 2021에서 적합하지 않는다**([train_offset.py:79](../test/train_offset.py#L79) = `season==2024`만).
   배포 최근접 전이 `2023→2024`에서는 **+0.1 = 죽음.**
   ⚠️ 더 나쁜 건, 그 반증(`2023→2024` V0 +26.0)이 **내 매트릭스에 이미 있었는데 안 봤다는 것**이다.
   채점 방식을 R만→전체로 바꾸면서 **배제 기준(2023 제외)을 안 고쳤다.**
5. 🔴 **offset 표 후보는 "여지"가 아니라 "지속성"으로 판정할 것** (2026-08-25 신설, §2-G)
   **여지(headroom)** = 그 축으로 나눴을 때 예측과 실제가 엇갈린 정도(이항노이즈 뺀 값).
   **지속성(persistence)** = 과거 연도에서 만든 편차 표가 목표 연도 편차와 갖는 상관.
   여지는 **재현되지 않는 진짜 변동**(그해 그 달의 폼 등)을 걸러내지 못한다. 이항노이즈
   보정은 "표본이 적어서 흔들린 몫"만 뺄 뿐, "실재하지만 내년엔 안 돌아올 몫"은 남긴다.
   ```
   축              여지     지속성 corr   전이가능    실측
   투수×좌우        126      0.412        54.8       LB +5.27  (021 채택)
   투수×월           93      0.049         6.8       —
   투수×주자          77      0.106         4.8       —
   투수×볼카운트      75*     —             —        0 (죽은 것이 확정된 축)
   ```
   \* 08 §5-12 표는 같은 축을 **0**으로 적었다. 노이즈 보정을 단순평균(문서) vs
   표본수 가중평균(내 재측정)으로 하느냐에 따라 2배씩 갈린다 — **여지는 절대값을
   믿을 수 없는 지표다.**
   🔴 **2026-08-25: 어느 규약이 옳은지 확정했다 — 가중이다**
   (`test/probe_pitcher_ceiling_fix.py`, 참신호 0인 가짜 데이터에서 가중 +4~7 ≈ 0 vs 단순 −50).
   따라서 08 §5-12 표와 `probe_pitcher_headroom.py`의 값은 **전부 과소평가**다:
   투수 단독 22 → **44**, 투수×좌우 126 → **209**, 투수 상수 천장 13 → **90**.
   ⚠️ 그래도 **채택 판정은 여전히 지속성으로** 한다 — 여지가 커져도 재현 안 되는 몫은 그대로다. 021이 채택근거 ①을 지속성 상관으로 둔 게 정답이었다.
   ⚠️ 이건 규칙 2의 새 사례다: **여지 = 대리 지표, 지속성 = 목적함수에 가까운 것.**
6. 🔴 **후처리 아이디어는 Murphy 분해로 REL/RES를 갈라놓고 볼 것** (2026-08-25 신설, §1-M)
   `Brier = REL − RES + UNC`. 우리 손실은 **RES가 REL의 148배**다 → calibration·클리핑·
   스케일 같은 **REL 도구는 구조적으로 6점 이상 못 준다.** 재기 전에 이 비율부터 확인.
   ⚠️ **REL이 작은 것은 성능이 아니다** — 상수예측도 REL=0이고 BSS는 0이다.
   ⚠️ REL은 **bin 개수에 민감**하다(같은 데이터에서 −4.7 ~ +6.0). 한 bin 수만 보고
   "0"이라 하면 규칙 2-a 위반이다. 실제로 그렇게 틀렸다.
7. 🔴 **세그먼트 BSS로 "손실이 어디 있나"를 판단하지 말 것** (2026-08-25 신설, §3-J 실패)
   세그먼트 BSS는 **그 세그먼트 자신의 `r(1−r)`로 나눈다.** 전역 점수와 무관하다.
   손실 위치는 **raw Brier의 전역 기여**로만 봐야 한다.
   ```
   실패 사례: F 행 세그BSS 602.3 (R은 909.5) → "F가 손실원, 올리면 +38" 이라 썼다
             실제 raw Brier  F 0.246820 < R 0.247620  → F는 **이득원**, 전역기여 **+33.3**
             F의 r=0.459라 U_seg가 작아 BSS만 낮게 보였을 뿐이다
   같은 이유로 "2스트라이크 전부 나쁘다"도 틀렸다 — 3-2는 전역기여 **+21.8**로 상위권.
   진짜 적자는 0-2(−47.8)·1-2(−37.8)·2-2(−16.3) 셋뿐.
   ```
   🔥 **후속(§2-G)**: F의 세그BSS가 낮은 진짜 이유까지 쟀다 — RES가 R의 92%인데
   원인이 **이력 부족**이다(`asof_pitcher_n` 중앙 F 895 vs R 3248). 이력량을 층화하면
   F 이득이 **−16.8**로 뒤집힌다. F 축 이론 천장 **+9.6**, 실현가능 0.
   ⚠️ 이건 규칙 2-b(목적함수 기여로 환산)의 재발이다. 규칙을 세워놓고 어겼다.
8. 🔴 **새 후보에는 이미 만든 판정 도구를 전부 돌릴 것** (2026-08-25 신설, §3-J 실패)
   `probe_offset_axis.py`에 **레벨/판별력 분리 테스트**(최적이동을 양쪽에 걸고 재기)를
   만들어놓고 `offset`에만 쓰고 `c-split`에는 안 썼다. 그 한 줄이면 +12.36 → −3.56이
   바로 보였다. **도구는 만든 순간부터 체크리스트다.**
9. 확인한 숫자는 **재현 코드와 함께** 남긴다. "대충 봤다"는 다음에 못 믿는다.
11. 🔴🔴 **판정 도구는 값만 낸다. 자동 기각 금지** (2026-08-25 신설, `gate.py` 자기검사)
   비용이 극단적으로 비대칭이다:
   ```
   위양성(틀린 걸 채택)  = 제출 1회 = 남은 예산의 1/40
   위음성(맞는 걸 버림)  = 그 축을 영영 잃음.  필요한 게 +85다
   ```
   그리고 **위음성이 큰 축에 몰려 있다.** 우리 점수를 만든 네 축 중 둘이 판별력 축이
   아니었다 — `shift`(레벨 보정, LB +47.04)와 `depth`(외삽 제어). 그런데 게이트의
   G2·G3가 정확히 그 둘을 거르도록 만들어져 있었다.
   ```
   실측: 구 G2("최적이동 후 잔존 ≥50%")에 shift 축을 걸면  Δ+40.00 → +0.00  잔존 0% = FAIL
        대조  platoon(LB +5.27) 100% PASS · 시즌내분해(LB +53.7) 106% PASS
   ```
   **G2의 전제가 틀렸다** — 배포 연도(2025)는 미관측이라 오라클 이동이 존재하지 않는다.
   우리가 +47.04를 얻은 방식이 바로 **그 이동량을 외부 지식으로 추정한 것**이었다.
   → G2는 게이트에서 빼고 `level_share()` 진단으로 강등. G1은 통과선(관측 1개로 정한
   0.40) 대신 **기존 관측점 옆에 놓아주는 것**으로 바꿨다. G3는 외삽 제어 후보에
   `extrapolation=True`를 주면 "판정 불가"를 낸다.
   ⚠️ **오늘 결정적이었던 것은 전부 판정이 아니라 숫자였다** — c분리 "shift 넣으면
   +0.37" · 0-2 "귀무 최고 5.74 > 실제 3.04" · F "raw Brier F<R". **라벨은 위음성만 만들었다.**
12. 🔴 **재구성한 베이스는 기록된 값과 대조할 것** (2026-08-25 신설, `gate.py` 버그)
   `gate.deploy_base()`가 021을 재구성하면서 **틀린 성공예측 캐시**를 읽고 있었다.
   ```
   artifacts/auxpred_ins             866.2 → 926.1   ← 018(ins 7열) 캐시. gate가 이걸 읽음
   artifacts/auxpred_ins_013_backup  859.0 → 912.4   ← 021 result.json 기록과 정확히 일치
   ```
   거기에 `platoon.csv`(2024 포함 배포용)를 그대로 2024에서 채점해 **자기적합 +77**까지
   붙어 있었다(1010 vs 기록 932.9). 하루치 측정이 잘못된 베이스에서 돌았다.
   ⚠️ **에러가 안 난다** — 캐시 이름이 비슷하고 숫자도 그럴듯하다. 재구성 직후
   `assert abs(got - 기록값) < 1.0`을 박아야 한다. `gate.py`에 넣었다.
   ✅ 재측정 결과 오늘 결론은 전부 유지됐다(F 천장 · 0-2 · RES 분해).

10. ❌ 기각도 지운다는 뜻이 아니다. 왜 안 되는지가 다음 아이디어의 제약이 된다.
11. 여기서 살아남은 것만 08의 실험 대장으로 올린다.

⚠️ 배경: **로컬 2024 ↔ LB가 783 부근에서 깨졌다**(08 §0-0). 새 피처는 로컬 점수로 채택
판정을 할 수 없다. 그러므로 이 노트의 가치는 "로컬을 올릴 후보 찾기"가 아니라
**"데이터 사실로 옳고 그름이 판정되는 후보만 남기기"** 에 있다.

---

## 1. 🔥 검증된 데이터 사실

### 1-A. `asof_*` rate 컬럼들은 배타적 분할이 아니다 (2026-08-11)

**계기**: "실패 = ①한복판 ②크게벗어남 ③포수반대 이므로 `1 − s − m − r` = ②" 라는 아이디어.
사용자가 "정말 합이 1이 되냐"고 물었고 — **아니었다.**

`asof_pitcher_n ≥ 200` & 결측 제외, 1,342,517행:

| 조합 | 평균 | sd | min | max |
|---|---|---|---|---|
| `success + middle + reverse` | **0.8933** | 0.0284 | 0.7426 | **1.0250** |
| `ball + strike` | **0.8133** | 0.0203 | 0.7008 | 0.9104 |

1. 합이 1이 아니다 (잔차 평균 0.107).
2. **최댓값이 1을 넘는다** → 배타적 분할이면 물리적으로 불가능. **범주가 겹친다.**

→ §1-D에서 원인 확정: **middle ∩ reverse = 50,266건** 실재.
→ ⚠️ **`asof_*` rate로 산술 파생을 만들 때 "합=1" 가정 금지.**

### 1-B. 🔥 `asof_pitcher_*`의 정의를 완전히 역공학했다

`open/data/first_game.csv`(2019 개막전, 265투구 — 모든 선수 이력 0에서 시작)로 관찰 후
전체 train 1,475,092행에서 검증.

```
asof_pitcher_n            = 그 투수의 과거 통산 투구 수. 0에서 시작해 정확히 +1씩.
                            시즌 리셋 없음. 경기 내에서도 투구마다 갱신됨. max 15,449.
asof_pitcher_success_rate = 그 투수 과거 투구 control_success의 단순 누적평균
                            (재현 최대오차 4.9e-07 = 소수 6자리 저장 반올림)
asof_pitcher_<c>_rate     = (과거 이벤트 수) / asof_pitcher_n,  c ∈ {middle,reverse,ball,strike}
asof_pitcher_<t>_rate     = (과거 이벤트 수) / asof_pitcher_pitchmix_n,  t ∈ 구종 3종
asof_pitcher_pitchmix_n  == asof_pitcher_n   (전체 train에서 항상)
row_id                    = 전체 train의 시간순 일련번호 (단조증가 확인)
```

→ **08 §8의 "시즌 리셋 없는 통산 누적" 주장 확정.**
→ ⚠️ rate는 **소수 6자리로 반올림 저장**돼 있다. 등식 검사 시 `1e-9` 같은 허용오차 쓰지 말 것
   (실제로 이것 때문에 첫 검증이 거짓 실패했다).

### 1-C. 🔥🔥🔥 숨은 투구 라벨이 복원된다

rate가 (정수 카운트 / n) 이므로 **연속한 두 투구의 카운트를 차분하면 그 투구의 이벤트가 나온다.**

```
cnt_k = round(rate_k × n_k)      →  k번째 투구의 이벤트 = cnt_{k+1} − cnt_k
```

**복원되는 라벨 8종** (전부 정확히 0/1): `middle` `reverse` `ball` `strike`
`fastball` `breaking` `offspeed` (+ 대조군 `success`)

**결정적 검증**: 이미 답을 아는 `success`를 같은 방법으로 복원 → **1,474,300행 전부
`control_success`와 일치.** 방법 자체가 증명됐으므로 나머지 라벨도 옳다.
추가로 `fastball+breaking+offspeed = 1` 이 100% 성립(완전 분할), `ball+strike ∈ {0,1}`.

- 결측 **792행뿐** = 투수당 커리어 마지막 투구 1개씩 (0.054%).
- 코드: [test/recover_labels.py](../test/recover_labels.py) → `test/recovered_labels.csv.gz` (4.7MB)
- 자가검증 assert 내장. 깨지면 즉시 멈춘다.

**⚠️ 사용 범위 제약 (중요)**
- ✅ **보조 타깃으로 쓰는 것** — 추론 시 라벨이 필요 없으므로 완전히 합법.
- ✅ **train(2019~24)에서 만든 정적 표를 `pitcher_id` 등으로 test에 join** — cond.py와 같은 방식.
- ❌ **새 as-of 피처를 만드는 것은 불가능.** 2025 test 행의 이력을 계산하려면 2025 라벨이
  필요한데 없고, test 행 간 순서 사용도 금지(규정 §5). 주최측이 준 `asof_*`만 쓸 수 있다.

### 1-D. 🔥 타깃의 논리 구조가 확정됐다 (1,474,300행, 예외 0)

```
middle  = 1  →  control_success = 0     예외 0건
reverse = 1  →  control_success = 0     예외 0건
control_success = 1 인데 middle 또는 reverse  →  0건
middle ∩ reverse = 50,266건             ← §1-A "합>1"의 원인
wayoff := (y=0 & middle=0 & reverse=0)  →  194,196건 (13.17%)   ← 세 번째 실패모드도 역산됨
```

**`control_success = 0 ⟺ middle ∪ reverse ∪ wayoff`.** 07 문서의 target 정의가 데이터로 확인됐고,
**세 성분이 전부 개별 라벨로 확보됐다.**

### 1-E. 🔥 시즌 드리프트가 성분별로 방향이 다르다

| 시즌 | 성공 | 한복판 | 반대 | 크게벗어남 | fastball | breaking | offspeed |
|---|---|---|---|---|---|---|---|
| 2019 | .5646 | .1250 | .1815 | .1569 | .5564 | .2857 | .1579 |
| 2020 | .5327 | .1482 | .2092 | .1418 | .5450 | .2919 | .1630 |
| 2021 | .5327 | .1437 | .2445 | .1195 | .5501 | .2863 | .1636 |
| 2022 | .5289 | .1495 | .2327 | .1254 | .5409 | .3025 | .1566 |
| 2023 | .5000 | .1533 | .2558 | .1251 | .5264 | .3040 | .1697 |
| 2024 | .4860 | **.1763** | **.2480** | **.1230** | .5306 | .3040 | .1654 |
| Δ | **−.079** | **+.051** | **+.067** | **−.034** | −.026 | +.018 | +.008 |

- **드리프트가 균일하지 않다.** 한복판·반대는 증가, **크게벗어남은 감소**.
- 크게벗어남은 2021 이후 사실상 평평(.1195/.1254/.1251/.1230). 추세를 가진 건 한복판·반대뿐.
- → **2025 외삽을 합계로 하는 것보다 성분별로 하는 게 정확할 수 있다** (§3-B와 연결).

### 1-F. 🔥🔥 보조 라벨의 신호가 타깃보다 10~140배 세다

η² = (조건부 평균의 분산) / (전체 분산). 조건: 카운트(12) / 상황(카운트×아웃×주자×이닝, 1,296) / 투수(792).

| 라벨 | 평균 | η²(카운트) | η²(상황) | η²(투수) |
|---|---|---|---|---|
| **`success` = 타깃** | .5237 | **0.00025** | **0.00149** | 0.00916 |
| middle | .1496 | 0.00150 | 0.00264 | 0.00384 |
| reverse | .2290 | 0.00411 | 0.00575 | 0.01305 |
| **wayoff** | .1317 | **0.01088** | 0.01273 | 0.00370 |
| ball | .3696 | 0.01356 | 0.01517 | 0.00406 |
| strike | .4433 | 0.00793 | 0.00999 | 0.00318 |
| **fastball** | .5414 | **0.03527** | **0.05394** | 0.03272 |
| breaking | .2958 | 0.01414 | 0.02301 | 0.05756 |
| offspeed | .1627 | 0.01229 | 0.01896 | **0.08496** |

**두 가지가 동시에 읽힌다.**

1. **모든 보조 라벨이 타깃보다 신호가 세다.** fastball은 카운트만으로 **141배**.
2. 🔥 **타깃은 세 성분의 합인데, 각 성분이 합보다 훨씬 예측 가능하다.**
   η²(wayoff)=0.0109 vs η²(y)=0.00025 → **44배.** 독립이라면 y의 η²가 성분들 수준이어야 한다.
   → **성분들이 서로 상쇄한다.** (0-2 카운트에서 투수는 존을 넓힌다 → 크게벗어남↑ 한복판↓,
   합치면 지워진다.) **지금 모델은 이 상쇄 구조를 거의 0인 신호로부터 암묵적으로 배우고 있다.**

**참고 상한**: 현재 투구의 구종을 안다면(카운트 조건부) Brier −0.000779 = **+313 BSS**.
실제로는 알 수 없으니 달성 불가능한 상한이지만, 구종이라는 축이 얼마나 큰지를 보여준다.
(100위까지 격차 = ΔBrier 0.000336 = 135점)

**구종별 성공률**: breaking **.4903** / offspeed .5135 / fastball **.5451** → **5.5%p 격차.**
**카운트별 구종 분포**: 3-0에서 fastball **93.8%**, 0-2에서 **45.0%** → 극심한 변동.

---

### 1-G. 🔥🔥 성분 분해가 [10 문서](10_KBO_시즌환경_분석.md)의 가설 두 개를 검증한다

⚠️ **§1-E 표는 R+F 혼합이었다.** 여기가 `game_type`으로 분리한 정확한 버전이다.

**R (1군)**

| 시즌 | 성공 | 한복판 | 반대 | 크게벗어남 |
|---|---|---|---|---|
| 2019 | .5495 | .1301 | .1884 | .1628 |
| 2020 | .5269 | .1443 | .2153 | .1473 |
| 2021 | .5128 | .1501 | **.2623** | .1195 |
| 2022 | .5037 | .1524 | .2586 | .1269 |
| 2023 | .5031 | .1577 | .2507 | .1229 |
| 2024 | .4897 | **.1785** | .2427 | .1214 |

**① 10 문서 §4 추론이 맞았고, 더 강하다.**
그쪽은 카운트 프로파일이 평평한 걸로 "실패 증가는 볼넷형이 아니다"를 *추론*했다. 직접 재보니
크게벗어남은 평평한 게 아니라 **−.041 감소**했다(.1628 → .1214).

**② 10 문서 §2-B의 ABS 메커니즘 가설이 성분 단위로 맞는다.**
가설: *프레이밍 무의미 → 포수가 미트 고정 안 함 → 투수가 존 가장자리 정직 공략 → 실투가 한복판.*

```
2024 (ABS 원년) YoY:  한복판 +0.0208 ← 6시즌 최대 단일 점프
                      반대   −0.0080  크게벗어남 −0.0015   성공 −0.0135
```
**하락이 거의 전부 한복판이다.** 반대가 **감소**한 것도 가설과 일치 —
프레이밍이 무의미하면 포수의 위치 요구 강도가 줄어 "요구 반대" 판정이 준다.

다른 해도 규칙 사건과 일대일 대응:
```
2021 (존 축소·볼넷 역대최고)  반대 +0.0470 (최대)   크게벗어남 −0.0278
2022 (존 정상화)              크게벗어남 +0.0074 (6시즌 중 유일한 증가)
2023 (1군 규칙 공백)          전 성분 미동 (최대 |0.0079|)
```

### 1-H. 🔴 F 아티팩트의 정체 — 지배 성분은 "반대"

**F (2군)**

| 시즌 | 성공 | 한복판 | 반대 | 크게벗어남 |
|---|---|---|---|---|
| 2019 | .6889 | **.0834** | .1249 | .1084 |
| 2020 | .5877 | **.1846** | .1512 | .0896 |
| 2021 | .7035 | .0894 | .0917 | .1193 |
| 2022 | .7086 | .1289 | **.0482** | .1148 |
| 2023 | .4728 | .1157 | **.2997** | .1444 |
| 2024 | .4590 | .1598 | .2879 | .1347 |

2022→2023 변화: 한복판 −.013 / **반대 +.252 (6배)** / 크게벗어남 +.030.

**가설**: pre-2023 퓨처스에는 **포수 요구(타깃) 기록이 없었다.** 그 실패 유형을 판정 못 했고,
못 한 만큼이 전부 "성공"으로 흘러갔다. → 10 문서 §3의 "라벨 파이프라인 교체"의 **내용물**.

**정량 검증** — F 성공률을 반대 성분 차이만큼만 보정하면 R과 맞아야 한다:
```
보정F = F성공 − (R반대 − F반대)
2019  .6254 vs R .5495  (+.076) ❌      2022  .4982 vs .5037  (−.006) ✅
2020  .5236 vs R .5269  (−.003) ✅      2023  .5218 vs .5031  (+.019) ✅
2021  .5329 vs R .5128  (+.020) ✅      2024  .5042 vs .4897  (+.015) ✅
```
**20%p 격차가 성분 하나로 거의 전부 설명된다**(2020~2024, ±0.02).
2019만 안 맞는데, 2019 F는 **한복판도 이상하다**(.0834 vs R .1301) → 두 성분 동시 부실.

📎 10 문서 §2-B의 "2020 F 0.588 이상값, 원인 미상"도 성격이 나온다 —
**한복판 스파이크**(.0834 → **.1846** → .0894). 2020 F 한정 한복판 라벨링 이상.

### 1-I. 🔥 pre-2023 F는 **전 성분의 구조가 3~7배 희석**돼 있다

카운트가 각 성분을 설명하는 정도(η²):

| 체제 | n | 성공 | mr | wayoff | 한복판 | 반대 |
|---|---|---|---|---|---|---|
| **R** | 1,313,704 | .00032 | **.00719** | **.01161** | .00173 | .00457 |
| **F_post2023** | 55,482 | .00018 | .00589 | .00909 | .00146 | .00404 |
| **F_pre2023** | 105,114 | .00032 | **.00102** | **.00383** | .00051 | .00143 |

3-0 ↔ 0-2 에서 성분이 실제로 움직이는 폭:
```
R            wayoff +0.150   mr −0.166
F_post2023   wayoff +0.126   mr −0.155     ← R과 사실상 같은 구조
F_pre2023    wayoff +0.072   mr −0.061     ← 절반 이하로 희석
```

**두 결론**
1. 🔥 **2023년 이후 F는 R과 통계적으로 같은 모집단이다.** → §2-B(모델 분리 기각)의 근거.
2. ⚠️ **pre-2023 F의 손상은 "반대" 하나가 아니라 전 성분에 걸쳐 있다.**
   §1-H의 평균값 이동은 반대가 지배적인 게 맞지만, **구조는 전반적으로 약하다.**
   단 **없는 게 아니라 절반 남아 있다** → 이 데이터가 양에 굶주려 있음을 감안하면
   **행 제거는 생각보다 위험하다**(§3-F).

### 1-J. 🔴 `asof`의 F 오염을 정량화했다

2024 R 경기에서 200투구 이상 던진 투수 210명. 각자의 `asof_pitcher_success_rate`와
**실제 2024 성적**을 비교:

| pre-2023 F 이력 비중 | 투수 | 투구 | 실제 2024 | asof | **편향** |
|---|---|---|---|---|---|
| 없음 | 73 | 91,670 | .4941 | .5055 | **+.0114** |
| ~5% | 77 | 76,049 | .4788 | .5069 | +.0281 |
| 5~15% | 38 | 25,532 | .4706 | .5052 | +.0346 |
| **15%+** | 22 | 12,432 | **.4705** | **.5468** | **+.0763** |

전체 가중 편향 **+.0221** / F이력 5%+ 그룹 **+.0444** (약 4배)

🔥 **순위가 뒤집혀 있다**: 2군 이력 15%+ 투수는 `asof` 기준 **전 그룹 1위(.5468)** 인데
실제로는 **꼴찌(.4705)** 다. 모델은 "2군 자주 가는 투수 = 제구 좋음"을 배우고 있다.
(야구적으로는 반대여야 한다 — 2군을 오가는 투수는 대체로 백업·신인이다.)

`없음` 그룹의 **+.0114 = 순수 시대 드리프트**(F 오염 없는 부분). §3-A가 잡으려던 것이 이것이고,
**F 오염이 그 위에 3배로 얹혀 있었다.**

⚠️ **§3-F(행 제거)로는 이게 안 풀린다.** `asof_*`는 주최측이 미리 계산해 준 컬럼이라
학습 행을 지워도 그 안의 2군 투구는 그대로다. **별도 보정 피처가 필요하다.**

### 1-K. 🔥🔥 트리는 2025를 2024와 **완전히 동일하게** 취급한다 (실측)

run 009의 배포 모델·계수를 그대로 써서 2024 행(253,507)에 대해 예측하고,
**`season`만 2025로 바꾼 사본**으로 한 번 더 예측했다.
([scratchpad/mu_effect.py](../test/) — 산출물은 임시)

```
season=2024   p_mr 0.3889   logit평균 −0.4606   mean(p_009) 0.4868
season=2025   p_mr 0.3889   logit평균 −0.4606   mean(p_009) 0.4868
              ↑ 소수점 5자리까지 완전히 동일
```

**한 자리도 안 움직인다.** "트리는 미관측 시즌을 외삽 못 한다"가 추론에서 **실측**이 됐다.
→ **§3-B의 근거가 가설에서 사실로 바뀌었다.**

#### 부수 결과 — `mu` 중심화가 만드는 이동은 **−0.0013뿐**

```
배포 계수:  b = −0.09283   c = +0.01753
            mu_mr = −0.51277 (확률 0.3745)   mu_wayoff = −1.98763 (확률 0.1205)

logit(p_mr) 평균 −0.4606  vs  mu_mr −0.5128   차 +0.0522
offset 로짓 이동 평균 −0.00514
mean(p_003) 0.4881  →  mean(p_009) 0.4868    차 −0.00126
```

⚠️ **정정**: 나는 이 이동을 −0.008로 추정하며 "run 009가 드리프트를 절반쯤 우연히
고치고 있다"고 썼다. **둘 다 틀렸다.**
- `mu`는 **비율의 로짓**(logit 0.3445 = −0.643)이 아니라 **예측 로짓의 평균**(−0.513)이다.
  보조 모델 예측이 중앙으로 수축돼 있어 훨씬 위에 있다.
- `b`도 −0.155(내 탐침값)가 아니라 **−0.0928**이다.

→ **원래 주장("run 009는 드리프트를 안 고친다")이 맞았고 정정이 틀렸다. 정정을 되돌린다.**
→ 교훈: run 009 산출물이 바로 옆에 있었는데 열어보지 않고 추정했다. **§0-1 위반.**

### 1-L. 🔥 드리프트는 **균일하지 않다 — 볼카운트가 유일한 비균일 축**

R만, 성공률의 시즌 변화를 축별로 갈라봤다.

| 축 | Δ19→24 sd | Δ23→24 sd | 판정 |
|---|---|---|---|
| **볼카운트** | **0.0203** | **0.0061** | 🔥 크다 |
| 이닝 | 0.0067 | 0.0061 | 약함 |
| 투수 실력 5분위(asof) | 0.0086 | 0.0030 | 약함 |

**볼카운트별 (Δ19→24 폭 0.0554 = 3배 차이)**
```
0-2  −0.0263   1-2 −0.0351   2-2 −0.0463      ← 2스트라이크: 덜 떨어짐
0-1  −0.0446   0-0 −0.0650   1-1 −0.0703
2-0  −0.0730   3-2 −0.0781   2-1 −0.0810
3-1  −0.0810   1-0 −0.0817                     ← 스트라이크를 넣어야 하는 카운트: 많이
```

🔑 **메커니즘이 있다**: 2스트라이크에선 투수가 유인구를 던지고, 그 행동은 존 규칙 변화의
영향을 덜 받는다. 반대로 스트라이크를 넣어야 하는 카운트에서 ABS발 한복판 실투가 늘었다.
→ §1-G의 ABS 가설과 같은 이야기가 카운트 축에서 다시 보인다.

**⚠️ 보조 모델에 시즌 상수를 넣는 것은 전역 이동과 수학적으로 중복이다.**
`logit(p_mr)`에 상수 `k`를 더하면 offset이 `b·k`만큼 **모든 행에서 동일하게** 이동한다
(`b`가 상수이므로). 전역 상수 이동과 같은 것이고, `b=−0.0928`이라 **10배 큰 상수**가 필요할 뿐이다.
→ 진짜 쟁점은 "어디에 넣느냐"가 아니라 **"상수냐 행별이냐"** 다.

🔴 **위 sd는 신호가 아니었다.** 제대로 검정하니 **최근 체제에서는 카운트별 차이가 없다** → §2-C.
단일 연도 차분(Δ23→24)은 두 비율을 빼는 것이라 노이즈가 √2배로 들어오고,
카운트 셀이 시즌당 약 2만 행이면 그 노이즈만으로 sd 0.005가 나온다. **관측된 0.0061의 대부분이 그것이다.**
→ §0-2 규칙(노이즈 바닥부터 비교) 신설의 계기. **긴 창(Δ19→24)의 이질성은 실재하지만
2019~2021 존 축소기가 만든 것이고 그 체제는 끝났다.**

### 1-M. 🔥🔥 격차의 정체 — **전부 해상도(RES)다. 후처리 축은 구조적으로 0** (2026-08-25)

재현: [`test/probe_brier_decomp.py`](../test/probe_brier_decomp.py)

`Brier = REL − RES + UNC` (Murphy 분해). [Bröcker 2008](https://arxiv.org/pdf/0806.0813)
- **REL**(신뢰도) "0.52라 말한 것 중 52%가 성공했나" = **거짓말한 몫**
- **RES**(해상도) 예측을 흩뜨려 **실제로 갈라낸 몫**
- **UNC** 상수예측의 Brier = `r(1−r)`. 데이터 성질, 모델 무관

#### 013 검증예측(전역 이동 후, BSS 893.6)

| bins | bin당 행 | REL−노이즈 | BSS |
|---|---|---|---|
| 5 | 50,623 | 0.0000023 | 0.9 |
| 10 | 25,311 | 0.0000101 | 4.1 |
| 20 | 12,655 | 0.0000151 | **6.0** |
| 50 | 5,062 | 0.0000019 | 0.8 |
| 100 | 2,531 | −0.0000116 | −4.7 |
| 200 | 1,265 | 0.0000056 | 2.2 |

**REL ≲ 6 BSS. RES = 0.0022327 = REL의 약 148배.**

⚠️ **처음에 100 bin만 보고 "REL은 정확히 0"이라 했다.** 그 칸은 노이즈 바닥(0.0000978)이
신호(0.0000861)보다 커서 음수가 나온 자리다. **한 bin 수만 보면 안 된다** → §0-6.

#### 🔴 REL이 작은 것은 성능이 아니다

```
                    REL          RES          BSS
상수예측(전부 .486)   0.0000000   0.0000000      0.0
우리 모델(이동 후)     0.0000151   0.0022327    893.6
```
**세상에서 제일 멍청한 모델도 REL=0이다.** 부스팅+147만행+전역 이동이면 자동으로 그렇게 된다.
예측이 base rate 근처에서 거의 안 움직이는데(sd 0.044) 틀릴 여지가 별로 없다.

#### calibration 축의 근거 교체

| isotonic (둘 다 2024 안에서) | Δ |
|---|---|
| 적합=채점 (부정행위) | **+34.8** ← 팀 노트북 "+75"의 정체 |
| **정직한 split-half** | **−19.0** ← 같은 연도인데도 손해 |

**고칠 게 6점뿐인데 보정기가 노이즈를 따라가며 그보다 많이 잃는다.**
기존 근거(팀 LB −23)는 제출 이름이 `calibration **and** smoothing` = **2변수 동시 변경**이라
교란돼 있었고, 로컬 교차연도 isotonic **+70.1**은 오히려 찬성 증거였다(08 §6-D).
→ **결론은 옳았으나 근거가 낡았다. 이제 "REL ≲ 6"이 근거다.**

#### 🔥 그래서 순위 격차의 정체

REL ≈ 0이므로 `RES − REL ≈ RES`. LB를 Brier로 환산하면:

| | BSS | RES−REL | 상수예측 대비 |
|---|---|---|---|
| 주최측 baseline | 549.51 | 0.001371 | +0.55% |
| **우리 021** | **1057.00** | **0.002637** | **+1.06%** |
| 100위 | 1124.70 | 0.002806 | +1.12% |
| 3위 *(제출 4회)* | 1218.66 | 0.003040 | +1.22% |
| **1위** | **1421.99** | **0.003547** | **+1.42%** |

🔴 **1위와의 −365는 우리가 틀려서가 아니라 못 갈라내서다.** 1위가 우리보다 **35% 더 갈라낸다.**
🔴 **후처리(calibration·클리핑·스케일·shrink)는 REL 도구다. 전부 상한 6점.**
🔴 **1위조차 상수예측보다 1.42% 나을 뿐이다** — 투구 단위 제구는 원래 거의 예측이 안 된다.
그 좁은 구간 안의 싸움이고, 이길 길은 **RES를 늘리는 것 = 새 판별 정보**뿐이다.

⚠️ 그동안 "새 정보가 필요하다"는 **추론**이었다(정보원 축이 다 닫혔으므로). 이제 **측정**이다.

### 1-N. 🔥🔥🔥 전이 배율의 진짜 결정 변수는 **베이스 포화**다 — §5-13 정정 (2026-08-25)

재현: [`test/probe_offset_axis.py`](../test/probe_offset_axis.py)

#### 08 §5-13의 분류가 틀렸다

§5-13은 배율 8개를 **"2025 미관측성을 건드리는 축"(0.79~2.40)** vs
**"판별력만 올리는 축"(0.26 이하)**으로 갈랐고 `offset`을 전자(2.40)에 넣었다.
사용자가 *"offset은 판별력을 올리는 앙상블 아니냐"* 고 물어서 **직접 쟀다**:

```
offset 없음                   BSS 769.4
offset                       BSS 791.4   Δ +22.0
offset 없음 + 최적이동         BSS 797.9
offset     + 최적이동          BSS 820.0   Δ +22.1   ← 레벨을 다 뺐는데 100% 잔존

예측 평균  0.49446 → 0.49447   (Δ +0.00001)  ← 레벨을 전혀 안 건드린다
예측 sd   0.04170 → 0.04620   (Δ +10.8%)    ← 퍼짐 = RES 증가
```

🔴 **offset은 순수 판별력 축이다.** 보조 과제 예측을 섞어 해상도를 올리는 스태킹이고,
드리프트와 무관하다. **그런데 배율이 2.40 = 역대 최고였다.**
→ **"판별력 축은 0.26이라 끝났다"는 결론이 무너진다.**

⚠️ §5-13은 결과변수(배율)를 보고 8개를 사후에 두 무리로 나눈 것이다. 그런 분류는
**거의 항상 "예외 없음"이 된다** — §0-5가 경고한 바로 그 구조다. 우리 문서에도 적용해야 한다.

#### 대안: 2요인 모형 (8개 전부 설명, 예외 0)

배율을 **얹은 베이스의 LB 점수** 순으로 세우면 완벽하게 단조다:

| 축 | 베이스 | 베이스 LB | 로컬 Δ | LB Δ | **배율** |
|---|---|---|---|---|---|
| offset | 003 | 881.73 | +26.6 | +63.7 | **2.40** |
| 시즌내 raw | 003 raw | 881.73 | +83.0 | +134.7 | **1.62** |
| 시드 3→7 | 009 | 945.40 | +3.8 | +5.6 | **1.47** |
| 시즌내 피처 | 012 | 998.00 | +75.3 | +59.2 | **0.79** |
| 좌우편차 | 015 | 1051.73 | +20.5 | +5.27 | **0.26** |
| mr 시즌내(030) | 021 | 1057.00 | +1.4 | −0.48 | **<0** |
| 시드 3→7 | 021 | 1057.00 | −1.4 | −0.52 | **죽음** |

```
① 새 정보인가?  아니면 → 베이스와 무관하게 음수      (cond, depth 8)
② 맞다면       → 배율이 베이스 강도에 따라 단조 감소  (2.40 → 0)
```

**같은 축(시즌내 분해)이 빈 베이스에선 1.62, 찬 베이스에선 0.79다.**
축의 성질이 아니라 **자리가 남았는지**의 문제다.
기전: 모든 축이 **같은 구멍**을 메운다 → 구멍이 찰수록 나중 축의 몫이 준다.
CLAUDE.md의 *"세 축은 독립이 아니라 부분 대체재"* 가 전체를 지배하는 법칙이었다.

🔴 **예측**: 현재 베이스(021, LB 1057)에서는 **무엇을 얹어도 배율 ≈ 0**이다.
025(−0.52)·030(−0.48)이 이미 그 예측대로 나왔다.

#### 그래도 이게 §5-13보다 희망적이다

포화는 **"우리가 넣은 것들끼리 겹친다"**는 뜻이지 **"더 넣을 게 없다"**가 아니다.
§1-M이 그걸 뒷받침한다 — **1위는 우리보다 RES가 35% 크다.** 구멍이 실재하고 비어 있고
**우리가 넣은 것들과 겹치지 않는다**(겹쳤으면 이미 메웠을 것이다).
→ 필요한 것은 **정말로 직교하는 정보 하나**. 그러면 포화 법칙이 적용되지 않는다.

#### 부수: offset 계수 축도 닫힌다

`b`의 목적함수가 최적 근처에서 극도로 평평하다 — ±20%에 **0.6점**.
연도별 `|b|`가 −.068 → −.091 → −.097로 커지지만, 그 추세를 2025로 외삽해
(−0.112) 2024에 써보면 **−0.37**. **"계수를 다르게 주는" 방향 전체가 죽었다.**
(형태 축은 08 §5-4에서 이미 전멸.)

## 2. ❌ 기각된 아이디어

### 2-A. ❌ 실패모드 3분해를 **뺄셈으로** 만들기 (`1 − s − m − r`)

기각 사유 2개, 각각 독립적으로 치명적:
1. §1-A/§1-D — middle과 reverse가 겹치므로(50,266건) 잔차는 "크게벗어남"이 아니다.
2. 타깃과 무상관: corr(resid, y) = **0.0077**(2023) / **0.0048**(2024)
   vs corr(asof_success_rate, y) = 0.0551 / 0.0582. **1/10 수준.**

📎 이때 잰 `resid R² = 1.0000 (from 5 asof cols)`는 **증거가 아니다.**
정의상 기존 열의 선형결합이므로 R²=1은 항등식이다. 이런 동어반복 지표를 근거로 쓰지 말 것.

⚠️ **단, "실패모드 분해"라는 방향 자체는 기각이 아니다.** 뺄셈이 틀렸을 뿐이고,
§1-C에서 **정확한 라벨을 얻었다.** → §3-D로 부활.

---

### 2-B. ❌ `game_type`별 모델 분리 (R 모델 / F 모델)

08 §0-4가 이미 기각했으나 **직접 측정이 아니라 추론**이었다("F를 빼면 −85.4 → 서로의
데이터를 뺏는 것이라 분리도 사장"). 추론 자체는 타당하다 — R 전용 모델이 곧 그 `ronly`
실험이고(−85.4), R이 test의 약 89%다. F 모델이 11%에서 +700을 벌어야 본전이다.

**여기서 근거 두 개를 더한다.**

1. **F 모델은 오염된 라벨로 학습된다.** 전체 F행 160,596 중 **pre-2023이 105,114 = 65.5%.**
   그런데 2025 F 행은 **정상 체제**를 따른다(10 문서 §3-F). 망가진 것 위주로 배워 정상인 걸
   예측하게 된다. 분리는 오염을 격리하는 게 아니라 **오염에 전용 모델을 쥐어주는 것**이다.
2. 🔥 **나눌 축이 아니다.** §1-I에서 **F_post2023 ≈ R** (η² mr .0059 vs .0072,
   wayoff .0091 vs .0116; 3-0↔0-2 이동폭도 거의 동일). 통계적으로 같은 모집단이다.
   진짜 경계는 `R vs F`가 아니라 **`pre-2023 F` vs 나머지**, 즉 **경기 유형이 아니라 라벨 체제**다.
   그건 `game_type × season`이고 **모델은 이미 둘 다 피처로 갖고 있다**(depth 6이면 표현 가능).
   → F 행을 빼면 −85.4인 것도 이것으로 설명된다: 모델이 이미 체제를 구분해 쓰고 있고,
   남는 건 데이터 양의 이득뿐이다.

### 2-C. ❌ 카운트별 드리프트 보정 — 최근 체제엔 **차이 자체가 없다**

§1-L에서 "드리프트가 카운트마다 다르다"를 보고 **카운트별 이동**을 검토했다.
카운트별 연간 기울기를 이항분산 기반 se와 함께 구하고 **이질성 검정**(Cochran's Q)을 했다.

| 창 | 공통 기울기 | Q | df | p | I² | 카운트 간 진짜 sd(τ) | **BSS 상한** |
|---|---|---|---|---|---|---|---|
| **2021~2024** (최근 체제) | **−0.00720** (se .00048) | 10.4 | 10 | **0.41** | **4%** | 0.00033 | **0.0** |
| 2019~2024 (전체) | −0.01090 (se .00026) | 108.0 | 10 | <1e-16 | 91% | 0.00273 | **3.0** |

- **최근 체제에서는 11개 카운트의 기울기가 통계적으로 구별되지 않는다**(p=0.41).
- 전체 기간의 이질성은 압도적으로 유의하지만(I²=91%), **1년 외삽 값어치는 3점뿐**이다.
  기울기 차이 τ=0.0027이 1년 뒤 편향으로 그대로 환산되기 때문.
  게다가 그 이질성은 **2019~2021 존 축소기**가 만든 것이고 그 체제는 끝났다(§1-L).

→ **전역 상수 하나가 맞다. 카운트별로 쪼개면 노이즈만 늘어난다.**

**✅ 그래도 남은 소득 — 이동량이 정밀해졌다**
```
공통 기울기 (2021~2024)   −0.00720 /yr   se 0.00048   ← 10 문서 §6-C의 독립 추정(−0.00699)과 일치
공통 기울기 (2019~2024)   −0.01090 /yr   se 0.00026

                      2025 R    F 10.5% 혼합   모델 0.4868 대비 편향    BSS
최근 기울기 −0.0072   0.4825    0.4786         +0.0082                  27
장기 기울기 −0.0109   0.4788    0.4753         +0.0115                  53
```
⚠️ 이전에 쓰던 −0.0120은 2019~2024 **연평균 차분**이었다. 최근 체제는 더 완만하다.

### 2-D. ❌ `ball`/`strike`/`inplay`/구종 — 그리고 **왜 안 되는지** (2026-08-12)
재현: [`test/probe_inplay.py`](../test/probe_inplay.py)

`inplay`(=18.71%)는 보조모델 BSS 2837로 `wayoff`(1553)보다 잘 학습되는데 기여는 +1.0~1.4.
`ball` +0.2 · `strike` +0.8 과 같은 자릿수. **기각.**

🔥 **8개 라벨을 가르는 선은 하나뿐이다** (예외 0):
```
mr · wayoff · middle · reverse   = 타깃의 **분해 성분**   → 기여 +8 ~ +26
ball · strike · inplay · 구종3    = 타깃과 무관한 라벨    → 기여 +0.2 ~ +1.4
```
→ **offset은 타깃 분해의 성분에만 작동한다.** 내 기준("`p_success`와 직교하면 후보")은
너무 약했다 — `wayoff`(−0.014)와 `inplay`(+0.012)가 둘 다 직교인데 +17 vs 0이다.

§1-D가 `y=0 ⟺ (M∪R) ⊎ W`를 147만 행 예외 0으로 확정했으므로 **성분은 정확히 2개이고
둘 다 쓰고 있다.** 더 찾을 라벨이 "없는 것 같다"가 아니라 **존재할 수 없다.**

### 2-E. ❌ offset **형태** 축도 닫혔다 (2026-08-12)
배포 조건(2022~2024 출처, 전체 채점)으로 재판정: **V0(현행) ≈ O1 ≈ O2a, 구별 불가.**
O3(w-blend)는 R만 채점에서 6/6이던 것이 전체 채점에선 2/4로 사망.

🔑 **남길 것 — 방향 구분**: 이상한 연도가 **목표**면 멀쩡하고(2024→2021 **+36.8**)
**출처**일 때만 깨진다(2021→2024 **−39.4**). 섞어서 "불안정"으로 오진했다. → 기준 4-5.
⚠️ 채점 범위(R만 → 전체)를 바꾸면서 배제 기준(2023 출처 제외)을 안 고쳐
`2023→2024 +26.0`을 표에 두고도 못 봤다. **기준을 바꾸면 딸린 배제 규칙도 같이 본다.**

→ offset은 라벨 축(§2-D)·형태 축(여기) 모두 닫혔다. **V0가 최종형.**

### 2-F. ❌ trackman 물리 프로필 — 매핑 46%→99%로 고쳐도 **없다** (2026-08-24)
재현: [`test/map_trackman.py`](../test/map_trackman.py) → `pitcher_map_seq.csv`

**영구 자산**: `row_id`가 시간순인 점을 이용한 게임 시퀀스 정렬로 매핑 46% → **99%**
(게임 4,094/4,868 유일매칭 · 투수 716/792 · 순도 .9877 · 충돌 0 · 커버리지 99.8%).

재검토 사유 둘(매핑 부실·평균만 씀)을 **다 해소하고도 부정**:
```
rel_side sd  ↔ 2024 성공률  −0.037   (asof +0.468)
17열 × out-of-year × 레벨제거   +120.7 BSS   ← 단, 베이스가 sm0 **1열**이었다
                               **실모델(013) 잔차로는 −10.3**
```
🔴 **남길 것 — 프로브 base가 낮으면 트리가 이미 잡던 몫까지 이득으로 센다.**
추정 이유: MLB 문헌의 결과변수는 볼넷·홈런인데 우리 타깃은 **주최측 운영 라벨**이다.

### 2-G. ❌ 투수×상황 offset — **클래스 전체 기각** (2026-08-25)
재현: [`test/probe_situation_headroom.py`](../test/probe_situation_headroom.py) ·
[`test/probe_gametype_axis.py`](../test/probe_gametype_axis.py) · 표준 도구 `gate.G1`

**측정법이 핵심이다 — 여지가 아니라 지속성.** 같은 해만 보면 모든 투수가 상황별로 다르다.
실력인지 흔들림인지는 **작년에도 같은 방향이었나**로만 갈린다(≤2023 표 vs 2024 표 상관).

```
투수×좌우 [대조] 0.412 ✅ | 주자 0.106 | 볼카운트 0.105 | game_type 0.056 | 월 0.049
타자수준 0.046 | 점수차 0.045 | 이닝 0.024 | 아웃 0.001 | leverage −0.010 | 홈원정 −0.052
7회이후 −0.034 | 타자경력 −0.166
```
표본 조건은 같다(좌우 51.5:48.5·중앙 344구 / 주자 51.9:48.1·중앙 339구) = **신호 부재**다.
2024 리그 평균도 좌우 .5009 vs .4703(3.1%p) / 주자 .4843 vs .4880(0.4%p).

> 🔥 **좌우만 이어지는 이유 = 그것만 신체 구조다.** 팔 각도와 타석 위치의 관계는 그 투수의
> 몸이라 안 변한다. 상대·상황·환경·시점은 전부 죽는다(**투수×타자수준 0.046**도 죽었다).

**부산물 — F는 손실원이 아니라 이득원이다.** raw Brier F .246314 < R .247369 → 전역 기여
**+43.9**. 세그BSS가 낮은 건 r=.459라 U_seg가 작아서다(§0-7). F 축 이론 천장 +9.6이고
`asof_pitcher_n`으로 층화하면 **−16.8** — F가 못 맞히는 건 이력이 적어서다(중앙 895 vs 3248).

⚠️ 08 §5-12의 "여지" 열을 믿지 말 것 — 이미 0으로 확정된 투수×카운트가 75.5로 나온다.
**여지는 "재현 안 되는 진짜 변동"을 못 거른다.** → §0 규칙 5, `gate.G1`.

### 2-H. ❌ §3-J 파생 3종 — 대체재이거나 다양성이 아니었다 (2026-08-25)
**(1) 죽은 보조 라벨을 2스트라이크 계수로 되살리기** — 현행 offset 위에서는 그럴듯했다
(`p_reverse` 전역계수 0.00 → 2S분리 **+15.41**, 계수 −0.0741/+0.1039로 부호가 반대라 전역 상쇄).
🔴 **§3-J(c 2S분리) 위에 얹으면 사라진다**(split-half): p_reverse −0.39 · p_middle −4.26 ·
p_fastball −1.05 · p_inplay +1.86 · p_breaking +4.76.
→ **c-split이 이미 그 영역을 먹는다.** `mr = M∪R`, `wayoff`가 여집합이라 2S의 실패모드
구성 변화는 어느 항으로 잡든 같은 것이다. (p_breaking은 §2-J에서 out-of-year 사망)

**(2) 013 × 018 피처셋 블렌드** — 018(66열)은 다른 열을 보니 다양성처럼 보였다.
로짓 상관 **0.99471**, 최적 가중 w013 = **1.00**. **다양성이 아니었다.**

**(3) ✅ 누수 감사** — platoon(021)은 세그먼트 전부 양수(유일한 −3.7은 노이즈). 무죄.
✅ c-split과 platoon은 가산적(891.2 → 915.4).

### 2-J. ❌ `breaking`을 offset 항으로 — **out-of-year 기각** (2026-08-25)
재현: [`test/probe_breaking_offset.py`](../test/probe_breaking_offset.py)

§2-D의 "구종 3종"은 실제로 **2단 혼합모델**(§3-E)만 잰 것이고 offset 항은 미측정이었다.
2024 캐시 예비값이 유망했다 — 전역 +6.23, **레벨 제거 후 +6.22**(§3-J를 죽인 테스트 통과),
K-fold OOF +5.2, 계수 안정. 남은 건 out-of-year 하나뿐이라 **기각선을 결과 전에 등록**했다.

```
2023→2024 (배포 최근접)  **−20.62**   c_brk −0.0572
2022→2024                 −0.67       +0.0008
c_brk 자기적합: 2022 +0.0008 / 2023 −0.0572 / 2024 −0.0203   ← 부호도 크기도 안 맞는다
```
⚠️ 2023 자기적합 +60.93은 **죽은 fold**(base 10.1)의 쓰레기. 사전 등록이 막았다.

🔴 **남길 교훈: in-year K-fold는 지속성 검사를 대신하지 못한다.** K=2·5·10 × 3시드가 전부
양수였는데 같은 해 안에서만 성립하는 관계였다. → **구종 축 완전 종료**(§3-E 상한 10.7과 정합).

### 2-K. ❌ 마지막 실적자 0-2·1-2 — **접근 경로 없음을 측정으로 확인** (2026-08-25)
재현: [`test/probe_2strike_residual.py`](../test/probe_2strike_residual.py) (학습 0회)

`gate.G5` 기준 진짜 적자는 **0-2(+51.4)·1-2(+42.0) 둘뿐**(F·3-2는 이득원).

**① 원인은 편향이 아니라 RES 부족** — 0-2의 RES가 1-1의 **1/3**이고 `pred sd`도 .0451 vs .0512.
**모델은 거기서 못 가른다는 걸 이미 안다.** REL은 어디서도 작다.

**② 원컬럼 37개 × 귀무 대조**:
```
        실제 최고   귀무 최고   95분위 초과
0-2       3.04     **5.74**     5/37 (기대 1.9)
1-2       7.77       4.65       9/37
```
🔴 **0-2는 구조 자체가 없다** — 귀무 최고가 실제 최고보다 크다. "합계 30.1"은 노이즈 합이었다.
⚠️ 귀무를 안 돌렸으면 `game_month` 3.04를 발견으로 착각했을 것이다.

**③ 1-2는 초과가 실재하나 후보가 지속성에서 죽는다**: `reverse_rate` corr **−0.736**(역상관),
`offspeed_rate` 부호 2/3. 그리고 **37열 × 2세그 = 74 측정 중 최고**를 고른 것이다(기준 #4).

### 2-L. ⚖️ 파라미터화 vs 번들링 — **원인은 번들링** (run 039, 2026-08-25)
재현: [`test/probe_param_vs_bundle.py`](../test/probe_param_vs_bundle.py)

사용자 지적: *"5전 5패 축이 전부 같은 형태(평균이동 표)로만 시도됐다."* 근거가 있었다 —
같은 정보가 형태에 따라 11.7점 갈렸다(004 트리 피처 번들 LB −5.4 vs 021 offset LB +5.27).

```
베이스              주모델    platoon b   얹은 뒤     이득
013 (기준)          859.0      2.253     887.5    +28.49
039 (+cond_ph)      872.9      1.500     885.5    +12.60
```
✅ **번들링이 원인이 맞다.** 트리는 `ph`를 쓸 수 있다(+13.9). 004는 죽은 표 3개가 희석했다.

🔴 **당시 결론 "최종값이 같으니 축은 안 열린다"는 2026-08-26에 반증됐다** — 044(같은 구성
풀스택)가 **LB +5.55**. 로컬 최종값 비교 자체가 편향돼 있었다(§2-R). 여기 남는 것은
**"번들링이 원인"** 부분뿐이다.

### 2-M. ❌ 분리 모델·샘플 가중치 — **홀대받는 부분집단이 없다** (2026-08-25)
재현: [`test/probe_res_concentration.py`](../test/probe_res_concentration.py)

**측정법 정정 2건이 먼저 필요했다:**
1. 🔴 **raw Brier 금지** — 세그먼트 고유 UNC가 섞인다. `plat`이 +88.9 적자로 보였는데
   두 그룹 `U` 차이(0.00087)가 관측 Brier 차이(0.00044)의 **2배**였다.
   통제 가능한 몫은 **`U_g − Brier_g`(설명몫)** 뿐. §0-7의 거울상.
2. **귀무 대조 필수** — 무작위 분할도 산포는 생긴다.

```
투수 개인 0.72x  좌우 0.45x  예측10분위 0.39x   ← 무작위보다 **덜** 퍼짐
투수 역할 3.55x  ← 유일한 초과였으나 "그 시즌 총 투구수" = **미래 정보**
                  leak-safe 대용(월당 소화량)으로 재니 귀무 범위 안
```
🔴 **가장 미세한 분할(투수 개인)에서 산포가 무작위보다 작다 → 목표물이 없다.**
⚠️ 축별 **주변** 집중도만 본 것이다. 셀 단위로만 드러나는 구조는 못 본다.

### 2-N. ❌ 세이버 지표 · n↔rate 짝 · PCA — 사용자 아이디어 3종 (2026-08-25)
재현: [`test/probe_sabermetric.py`](../test/probe_sabermetric.py)

**① 세이버 지표 — 주최측이 이미 줬다.** 인접 행 차분으로 복원은 된다(아웃 54.9% · 득점 12.1%
· 출루 24.0% · 판별불가 12.6%, 투수 465명). 그런데 021 잔차에 **7개 전부 자기 귀무 이하**.
```
K_rate  ↔ asof_pitcher_strike_rate  **+0.898**      BB_rate ↔ asof_pitcher_ball_rate **+0.908**
KBB     ↔ asof_pitcher_strike_rate  **+0.942**      FIPish  ↔ 같은 열 −0.801
```
→ 판별식 ①에서 죽는다. ⚠️ 결과 라벨(안타·실점)을 보조 타깃으로 쓰는 변형도 안 된다 —
offset이 먹히는 이유는 `p_mr`·`p_wayoff`가 **타깃의 성분**이기 때문이고 안타는 **하류 결과**다.

**② `n`↔rate 짝 — 전제가 없다.** `asof_pitcher_n == asof_pitcher_pitchmix_n`
**100.0000%**(1,475,092/1,475,092). 같은 열이다. 그리고 `n<100`인 행은 3.12%뿐이고
08 §5-16이 이미 쟀다(피처 수준 69 → **모델 잔차 2.4**).

**③ PCA — 측정 없이 기각.** 비지도(η²(y)=0.00025라 상위 성분에 안 남음) · 축 회전이 트리
분할을 깸(`cat_features` 지정이 +114였다 = 축을 살린 게 최대 이득) · 범주형 8개를 못 태움.
⚠️ "피처가 많다"의 검정 가능한 형태는 PCA가 아니라 **삭제 실험**이다.

### 2-O. ⚖️ 월당 소화량 — **지속성 통과, 전이 실패.** 그리고 내 기각 사유가 틀렸다 (2026-08-25)

사용자 질문: *"왜 자꾸 귀무가 다 기각해버리는거지?"* → **과잉 기각이 맞았다.**

#### 내 원래 판정이 틀렸다

§2-M에서 `ins_pitcher_n / 경과 개월`(월당 소화량)을 **"귀무 범위 안 = 미래정보 아티팩트"**로
닫았다. 두 군데가 잘못됐다:

1. **`max(귀무)` 기준을 사전 지목 가설에 썼다.** 그건 여러 개를 훑었을 때(family-wise) 쓰는
   기준이다. 이건 역할 발견에서 **유도한 단일 가설**이라 95분위가 맞는 바다.
2. **분위 수를 하나만 봤다** (§0-2 위반). 거칠게 나누면 max도 넘는다:
```
분위    실제    귀무평균   95분위    최대       판정
  3    47.55    15.66    30.12   44.74   ✅ max 초과
  5    31.20    14.67    23.93   34.35   ⚠️ 95분위만
 10    16.25    11.69    15.90   17.70   ⚠️
 20    10.63     8.49    10.71   12.73   ❌
```
거칠수록 강해지는 것은 **매끄러운 저차원 효과**의 서명이다(고운 분할이 신호를 쪼개고
노이즈를 더한다). 노이즈였다면 이런 단조성이 안 나온다.

#### 지속성은 통과한다 — 좌우의 두 배

```
월당 소화량 5분위별 편향(실제−예측), 전역 최적이동 후
        Q1       Q2       Q3       Q4       Q5
2021  +0.0049  −0.0018  −0.0037  −0.0009  +0.0014
2022  −0.0157  +0.0038  +0.0021  +0.0032  +0.0066
2023  −0.0260  −0.0031  +0.0068  +0.0010  +0.0214
2024  −0.0027  −0.0020  +0.0023  +0.0001  +0.0023

Q5−Q1 부호 **3/3**   ≤2023 프로파일 vs 2024 corr **+0.824**   (투수×좌우는 0.412)
```
방향도 해석된다 — **적게 던지는 투수는 과대예측, 많이 던지는 투수는 과소예측.**

#### 🔴 그런데 배포 조건에서 죽는다

```
S에서 적합 → T에 적용 (분위 경계도 S에서)
              기준(전역이동)   +분위보정       Δ
3분위 2023→2024   798.3       753.1     **−45.20**  ← 배포 최근접
     2022→2024   801.7       797.3      −4.35
5분위 2023→2024   798.3       719.5     **−78.74**
     2022→2024   801.7       791.2     −10.54
     2022→2023    28.3        80.2     +51.85  (목표 = 죽은 fold, 제외)
```

**원인: 모양은 이어지는데 크기가 안 이어진다.**
```
진폭(Q5−Q1)   2022 0.022 · 2023 0.047 · 2024 **0.005**   ← 10배 흔들린다
```
2023(큰 진폭)에서 적합해 2024(작은 진폭)에 걸면 **10배 과보정**한다.
08 §6-J가 기록한 그 실패다 — *"금지할 것은 사후 이동이 아니라 **이동량의 fold 적합**"*.

#### 🔥 남길 것 — §0-5에 단서가 붙는다

지금까지 죽은 축은 **대부분 지속성에서** 죽었다. 이건 **지속성을 통과하고 전이에서 죽은
첫 사례**다. 그러니:

> **모양의 지속성만으로는 부족하다. 크기(진폭)도 이어져야 한다.**
> `corr`는 프로파일을 표준화해서 재므로 진폭 정보를 버린다. **`corr`와 함께 연도별 진폭을
> 반드시 볼 것.** 좌우(021)가 통과한 이유에는 진폭 안정성도 있었다.


### 2-P. ❌ 투수 역할 표(등판당 투구수) — 레벨도 피처도 실패 (run 040, 2026-08-25)
재현: [`test/probe_role_table.py`](../test/probe_role_table.py) · `common/role.py`

사용자 제안대로 **합법**이다(`platoon.csv`와 같은 형태). 표 자체도 안정적이다
(≤2023 vs 2024 corr **+0.741**, 등판당 투구수 중앙 20 / 75% 48).

🔥 **그런데 corr +0.838인데 진폭 부호가 1/3이다**(변동폭 7.5배). 상관은 프로파일을
표준화하므로 중간 점이 지배하고, 정작 보정에 쓰이는 방향과 크기는 해마다 뒤집힌다.
전이 `2023→2024` **−8.08**(배포 최근접) / `2022→2024` +10.86. → **레벨 형태 기각.**

트리 피처로 줘도 진다(run 040, 주모델 **−11.0**). 🔴 **결정적 증거 — 표가 없는 행이
더 나빠졌다**(있음 −9.9 / **없음 −15.0**). 결측 행은 그 열의 직접 영향이 없는데도 나빠졌다
= **열을 추가한 것만으로 트리 분할이 전역적으로 망가졌다.** `best_iter`도 일찍 끊겼다.

#### 🔥 남길 것 — 피처 채택 진단 패턴
```
진짜 신호   주모델 ↑  +  하류(offset) 이득 ↓      039: +13.9 / 28.49→12.60   → LB +5.55
노이즈 열   주모델 ↓  +  하류 이득 그대로          040: −11.0 / 28.49→30.70
```
⚠️ 2026-08-26 갱신: 이 패턴은 LB 2점에서 맞았지만 **전수로는 3/5**다. → CLAUDE.md §7-0.

### 2-L. ❌ 시즌내 구종 믹스 · 투수 기복 — 그리고 **오늘의 진짜 발견** (2026-08-25)
**(1) 시즌내 구종 믹스 분해** — 018의 5열과 분모(`pitchmix_n`)도 성격(결과가 아니라 **행동**)도
달라 미시도였다. shift sd 0.05~0.06으로 크게 움직이는데 **계수 부호가 뒤집힌다**
(2022 +0.004 / 2023 −0.231 / 2024 −0.129). 기전 추정: **더 던지기 시작한 것은 먹혀서**다.

**(2) `asof`가 안 주는 투수 통계**(기복·상황프로필·등판부담) — `asof`엔 **분산이 없다.**
🔴 **레벨을 안 걷으면 완전히 속는다**: shift 없이 재면 여지 40.9 · volatility +31.4.
shift를 넣으면 여지 14.8 · 절편만 +0.24. 그제야 `volatility` +5.10만 남는데 **out-of-year 사망**
(지속성 corr +0.199 / **+0.006** / +0.086, 전이 2023→2024 **−26.02**).

#### 🔥🔥 오늘의 진짜 발견 — **in-year는 전부 통과하고 out-of-year에서 전부 죽는다**
| 후보 | in-year | out-of-year / 지속성 |
|---|---|---|
| c 2스트라이크 분리 | +12.36 | 레벨 제거 **−3.56** |
| `p_breaking` offset | +5.2 (K-fold) | **−20.62** |
| 시즌내 구종 믹스 | 자기적합 +4.7 | −0.05 ~ +0.92 |
| 투수 기복 | +5.10 (투수분할 CV) | **−26.02** |
| 투수×주자 · 투수×월 | 여지 77 · 93 | corr 0.106 · 0.049 |
| 013×018 블렌드 | — | 상관 0.99471 |

🔴 **in-year 교차검증(K-fold·투수분할·split-half)은 지속성 검사를 대신하지 못한다.**
연도를 고정하면 그 해의 노이즈 실현이 구조처럼 보인다. **반드시 연도를 건너서 잰다.**
⚠️ 죽은 fold 2023 자기적합이 이날만 두 번 튀었다(+60.93 · +104.95).

## 3. ⭐ 유망 후보

> 공통 배경: 08 §3이 "시즌 base rate 하락"을 **"가장 중요한 발견"** 이라 적어놓고
> 실제 대응은 "검증셋을 2024로" 하나뿐이었다. 46개 실험이 전부 드리프트를 트리에게
> 맡긴 채 그 위에서 미세조정했다.

### 3-D. ✔️ 다중 타깃 분해 — **채택됨 (run 009, LB 945.40 → 전이 +63.7)**

배포 형태는 메인 세션의 제약형(`a=1·d=0` 고정, `mu` 중심화, 연도 간 적합) — 08 §5-1~5-3.
형태 축 판정은 08 §5-4. **여기는 발견 과정만.**

발단(§1-F): **성분들은 합보다 10~140배 예측 가능하고, 합쳐질 때 상쇄된다.**
정보 이득이 아니라 **통계적 효율** — 타깃 η²(y)=0.00025에 147만 행이면 그게 병목이다.
08의 앙상블 실패들과 다른 점: 그것들은 "같은 타깃·같은 피처"였고 이건 **다른 타깃**이다.

**🔬 1차 — 라벨별 학습 가능성** ([`test/probe_aux_labels.py`](../test/probe_aux_labels.py))

| 라벨 | base rate | BSS | AUC |
|---|---|---|---|
| **`success` (기준)** | .4860 | **772.1** | 0.5499 |
| middle | .1763 | **610.9** ⚠️ | 0.5741 |
| reverse | .2480 | 1394.0 | 0.5776 |
| wayoff | .1230 | 1553.0 | 0.6021 |
| **mr (M∪R)** | .3910 | **1615.7** | 0.5753 |
| breaking | .3040 | **12698.2** | **0.7229** |

η²이 실제 예측력으로 이어진다(wayoff·mr 2배, 구종 12~16배).
⚠️ **`middle`만 기준보다 낮다(610.9)** — η²(카운트)는 타깃의 6배인데 실제론 더 못 맞춘다.
그래서 `mr`로 합쳤다. (합친 이유가 "상관이 낮아서"라고 썼던 것은 틀렸다 —
`middle↔reverse` 로짓 상관은 0.162로 낮고, 진짜 원인은 `middle↔wayoff` **−0.610**이다.)

❌ **보조 예측만으로는 타깃을 재구성하지 못한다**: success 단독 838.6 vs mr+wayoff 794.0.
→ 대체가 아니라 **덧셈**이어야 한다.

**🔬 2차 — 타깃 위에 얹기**

| 결합 | 선형(로짓) | GBDT |
|---|---|---|
| **`success` 단독** | **838.6** | 821.0 |
| `success + mr + wayoff` | **867.0 (+28.4)** | 835.7 |
| `success + 구종2` | 839.2 (**+0.6**) | 812.6 |
| `success + 전부` | 869.9 (+31.3) | 869.9 |

✅ **실패모드 분해는 없던 정보를 더한다 (+28~31).**
❌ **구종은 기여가 없다 (+0.6)** — AUC 0.72로 가장 잘 학습되는데 타깃 위에선 0.
🔑 **GBDT 결합이 선형보다 나쁘다**(기준선부터 821.0 < 838.6) → 결합은 **로짓 선형**으로.

**🔬 3차 — 메인 세션 지적 반영**

R만으로 재채점(fold 2021·2022의 F/R 격차가 만든 공짜 점수 제거):
`success` 800.7 → `+mr+wayoff` 819.2 = **+18.5** (전체 기준 +28.4에서 축소되나 부호 유지).

계수 전이 위험을 피하려 **계수 0개 형태**도 시험했다:
`p_struct = 1 − p_mr − p_wayoff` 단독 746.8 / **w=0.6 혼합 784.0 (+13.9)**.
→ 제약형(`a=1·d=0`, 계수 2개)이 더 좋아 그쪽이 채택됐다.
⚠️ 항등식 형태도 **base rate를 +0.008 과대추정**한다 — 드리프트는 따로 고쳐야 한다(→ §3-B, shift).

### 3-E. ❌ 구종 잠재변수 2단 모델 — **구조적 상한 10.7** (2026-08-25)
재현: [`test/probe_pitchtype_ceiling.py`](../test/probe_pitchtype_ceiling.py)

**전제는 맞다.** 복원 라벨로 2024 실측 — 변화구가 정말 어렵고 모델이 실제로 틀린다:
```
변화구 30.4%  실제 .4516  013 예측 .4893   **+.0376 과대예측**
직구   53.1%       .5024        .4944       −.0080
체인지 16.5%       .4968        .5094       +.0125
격차 5.1%p (좌우 3.1%p보다 크다)   **오라클 = +160.5 BSS** (투수×좌우 126보다 크다)
```

**그런데 test에서 구종을 모른다.** 쓸 수 있는 형태는 `P(구종|x)`뿐:
```
조건                      셀      sd(조정량)  ΔBSS   오라클 대비
투수 믹스만 [기존 컬럼]     10       0.0034     4.7     2.9%
카운트만   [기존 컬럼]     12       0.0033     4.5     2.8%
카운트×믹스              120       0.0048     9.3     5.8%
카운트×믹스×상황       1,080       0.0052    10.7     6.7%   ← 상한(낙관)
오라클(진짜 구종)                            160.5    100%
```
🔴 **6.7%밖에 못 건진다. 이유가 데이터의 성질이다:**
```
breaking 라벨 자체 sd        0.4600   ← 오라클에 필요한 흔들림
P(breaking|카운트×믹스) sd   0.1187   ← 실제로 얻는 흔들림
```
그 10.7마저 재료가 기존 컬럼(`asof_pitcher_breaking_rate`·카운트)이라 트리가 `E[y|x]`를
적합하는 순간 중간 경로로 들어와 있다. 실측: 013 잔차를 변화구비율 10분위로 → **여지 +2.1**.
두 경로 일치: 10.7 × 전이율 0.096 ≈ +1.0 vs 실측 +0.6.

⚠️⚠️ **이 축에서 새 시도를 검토하면 `P(구종|x)`의 sd 0.119를 먼저 볼 것.**
그 흔들림이 안 커지면 상한도 안 커진다. **표의 진폭(피처 흔들림)과 혼동하지 말 것** —
2026-08-26에 `cond_phb`(투수×타자손 구종표)를 "지속진폭 0.40 = success의 12배"라고
제안했다가 주모델 **−5.8**로 확인했다. 재는 양이 달랐다. ⚠️ 단 이 상한 계산의 셀 정의에
`batter_hand`는 없다(`probe_pitchtype_ceiling.py:49-58`) — 손별 분해를 직접 bound하진 않는다.

### 3-A. ✔️ era 보정 asof — **채택됨** (메인 세션이 `시즌내 성적 분해`로 구현, run 013)

> 🔥 **단일 피처 축 역대 최대: 로컬 +75.3 / LB +53.7.** 최종 형태는 시대 구성 가중이 아니라
> **직전 시즌 말 기준점 빼기**: `(n₁r₁ − s₀)/(n₁ − n₀)` → 통산의 시대 편향(2024 +.0253)이
> **+.0033**으로. `n₀·s₀`는 모델이 도달 불가능한 값 = **새 피처가 아니라 새 정보.**
> 아래는 이 축을 제안했을 때의 원 기록이다.

`asof_pitcher_success_rate`는 최강 피처인데 **시대가 섞여 오염돼 있다.**
2019년(리그 .565)의 통산 .52 = 평균 이하 / 2024년(리그 .486)의 .52 = 평균 이상. 모델은 같게 본다.

§1-B에서 **전제가 확인됐다** — `asof_pitcher_n`은 시즌 리셋 없는 통산 누적이고 정확히 +1씩
증가하므로, train에서 투수별·시즌별 투구수를 세면 asof 창의 시대 구성을 **정확히** 계산할 수 있다.

```
expected_rate    = Σ_s (그 투수의 시즌 s 투구수 × 리그평균_s) / 전체
asof_skill_delta = asof_pitcher_success_rate − expected_rate
in_season_n      = asof_pitcher_n − (train에서 센 ≤2024 통산)   ← 2025 시즌내 워크로드
```
부산물: 데뷔 시즌 / 경력 연차 / 신인 여부.
규정 ✅ (train + 그 행 자체만). §3-G trackman 사장 사유("정보 중복")와 달리 **노이즈 제거**다.

남은 미확인: `league_rate_2025`를 뭘로 둘 것인가 (추세 외삽 vs §3-C 실측 vs 10 문서 §6-C 추정 0.477).

🔴 **설계 정정 (2026-08-11, §1-J 근거)**: 기준선을 **`season`만으로 잡으면 안 된다.**
같은 2022년에 R은 .5037, F는 **.7086**이다. 2군 등판 이력이 많은 투수일수록 `asof`가
부풀려져 있고, 그 편향이 순수 시대 드리프트(+.0114)의 **4배(+.0444)** 다.
```
❌ 리그평균_s                    (season만)
✅ 리그평균_{s, game_type}        + pre-2023 F는 기준선 계산에서 제외
```
§3-F(행 제거)로는 안 풀린다 — `asof_*`는 주최측 사전계산 컬럼이라 학습 행을 지워도
그 안의 2군 투구는 남는다. **보정 피처로만 접근 가능.**

### 3-B. ✅❌ league-rate offset — 평균분 **완료**(+47.04), 교호작용분 **기각**(상한 3점)

08 §5-7이 남긴 질문: *"§5-6이 상수로 평균을 고쳤으니 남은 건 드리프트가 상황마다 다른 부분.
추가 이득은 교호작용뿐이고 0일 수 있다. 비용 = 메인 전체 재학습 2시간."*

**재학습 없이 판정했다.** 축별로 연간 기울기의 이질성 검정(Cochran's Q).
1년 외삽 시 편향이 곧 τ(칸 간 진짜 sd)이므로 **BSS 상한 = τ² × 401,445.**
대상 881,587행(1군만, 2021~2024 = 최근 체제).

| 축 | 칸 | 공통기울기 | p | I² | τ | **BSS 상한** |
|---|---|---|---|---|---|---|
| month | 6 | −0.00921 | 0.031 | 59% | 0.00152 | **0.9** |
| exp(경력 5분위) | 5 | −0.00840 | 0.060 | 56% | 0.00125 | 0.6 |
| count_state | 12 | −0.00707 | 0.134 | 32% | 0.00116 | 0.5 |
| inning | 9 | −0.00693 | 0.144 | 34% | 0.00103 | 0.4 |
| runners | 4 | −0.00701 | 0.171 | 40% | 0.00086 | 0.3 |
| skill 5분위 | 5 | −0.00219 | 0.308 | 17% | 0.00049 | 0.1 |
| leverage | 4 | −0.00693 | 0.310 | 16% | 0.00042 | 0.1 |
| margin · base_state · outs | 5·8·3 | −0.0069~0.0070 | 0.44~0.87 | **0%** | 0 | **0.0** |
| | | | | | **합계(독립 가정)** | **3.0** |

🔥 **드리프트는 모든 상황에서 균일하다** — 어느 축으로 쪼개도 연 **−0.0069 ~ −0.0092**.
§5-6의 상수 하나가 이미 전부 먹었고, **남은 교호작용은 상한 3점.**
→ 2시간 재학습 + 메인 모델 변경 위험을 감수할 값이 아니다. **기각.**

📎 `skill` 축의 공통기울기가 −0.00219로 유독 작은 것은 발견이 아니다 —
`asof` 5분위가 **상대 순위**라 드리프트를 자체 흡수한다(교란).

트리는 `season≥2024` 잎을 2025에 재사용한다. **추세를 알아도 외삽 못 한다.**
→ `logit(p) = logit(league_rate(season)) + f(x)` (CatBoost `baseline` / LGBM `init_score`).

- 표현력 증가가 **아니다** → 08 §0-0 "용량 늘리지 말 것" 제약 통과. **지금 가장 안전한 축.**
- §1-E 덕에 **성분별 offset**도 가능해졌다 (3-D와 결합 시).
- 검증: BSS 말고 **`mean(p) − 실제 r` 편향**을 본다. fold 노이즈에 덜 흔들린다.

### 3-C. ⭐⭐ LB δ 프로브 (피처 아님. 3-A/3-B의 입력을 준다)

균일 상수 δ를 더한 제출의 Brier는 정확히 2차식: `B(δ) = B0 + 2δ(mean(p) − r) + δ²`.
같은 행·같은 예측이라 **샘플링 노이즈 0.** δ ∈ {−0.02, 0, +0.02} 3제출
→ 최적 오프셋 + **2025 실제 base rate r**.

⚠️ 사전에 예측을 `[0.02, 0.98]`로 클립해야 상수 이동이 정확히 성립.
⚠️ 규정 회색지대 — "평가 데이터 보고 만든 사후 보정" 조항. 팀 판단 필요.

---

### 3-F. ❌ 보조 모델 학습에서만 pre-2023 F 제외 — **기각** (08 §5-5)

> 실측: 정제 효과가 **2021 출처에서만** +160·+87이고, 깨끗한 F가 하나라도 섞인 출처
> (2022·2024)에서는 **−9.5 ~ −1.2로 진다.** 배포는 깨끗한 시즌이 2개(2023·2024)라 더 나빠진다.
> → 09 §2-E의 O2a와 **같은 함정**(2021 출처가 만든 이득). 같은 날 같은 실수를 두 번 제안했다.
> 🔴 이로써 **"행을 지우는" 처방이 세 번 다 실패**했다(F 전체 −85.4 · 옛 시즌 삭제 · aux_clean). 재시도 금지.

**run 009의 보조 모델이 오염된 라벨로 학습되고 있다.**
```
mr 라벨 평균     R .3527   F_pre2023 .2154   F_post2023 .3954
카운트→mr 구조   R .00719  F_pre2023 .00102              ← 7배 약함
```
run 009는 `p_mr`·`p_wayoff`에 얹혀 있는데, 그 모델들은 train 전체로 학습됐고 거기엔
**`mr`이 계통적으로 과소기록된 105,114행(7.13%)** 이 섞여 있다. 하필 **보조 모델이
예측하려는 바로 그 라벨**이 망가진 행들이다.

```
보조 모델(p_mr, p_wayoff) 학습에서만 pre-2023 F 행 제외.  메인 모델 무수정.
```

| | 메인에서 제거(§3-G) | **보조에서만 제거(3-F)** |
|---|---|---|
| 데이터 손실 | 7.13% ⚠️ −85.4 위험 | 7.13% |
| 그 행의 라벨 상태 | `control_success`는 부정확하나 **구조 절반 남음**(§1-I) | **`mr`이 정면으로 틀림** |
| 판단 | 양이 이기는지 불확실 | 노이즈 제거가 명확 |

- 메인 모델을 **손도 안 대므로 −85.4 위험이 아예 없다.**
- run 009 대비 **단일 변수**, 용량 증가 0.

### 3-G. 📎 메인 학습에서 pre-2023 F 제거 — **하향** (원 §3-F 후보)

10 문서 §3-F가 제안한 것(`season ≤ 2022 & game_type == 'F'` 105,308행 = 7.14% 제거).

⚠️ **§1-I로 하향한다.** 이전에 "pre-2023 F는 반대 성분만 망가졌다"고 봤으나, 재보니
**전 성분 구조가 3~7배 희석**된 대신 **없는 게 아니라 절반 남아 있다**(3-0↔0-2 이동폭이
R의 40~50%). 08 §0-4가 "이 데이터는 양에 굶주려 있다"를 −85.4로 실증했으므로,
**반쯤 쓸모있는 10만 행을 버리는 것은 같은 함정에 걸릴 수 있다.**
→ 같은 통찰을 **위험 없는 곳에만** 적용하는 3-F를 먼저 한다.

---

### 3-I. ❌ offset **형태** 축 — 전멸 (판정 전문은 §2-E)

제안 형태 전부(V0~V6, `both`, O2a) 기각. 근거·숫자는 [§2-E](#2-e)와
[08 §5-4](08_Phase2_데이터_및_전략.md)에 있다. 여기 중복분은 제거했다.

남는 교훈 하나: **`both`(M∩R) 항이 `2022→2024` 한 측정에서만 이겼다.** 246조합을 그 하나로
줄 세우니 상위 15개가 전부 `both`를 포함해 "구조적 신호"처럼 보였고, 전이를 6개로 늘리자
사라졌다(2022→2021 +30.8 vs +8.0 · 2021 출처면 −1669). → 채택기준 #4의 실증 사례.

### 3-J. ❌ offset `c`를 2스트라이크로 분리 — **기각** (제안 후 같은 날 반증, 2026-08-25)

재현: [`test/probe_offset_c_split.py`](../test/probe_offset_c_split.py)

제안 근거는 잔차 지도였다 — 2스트라이크 카운트의 세그BSS가 유독 낮고(0-2 246 vs 3-0 1516),
전역 offset이 거기서 **−29.7**로 새고 있었다(그 외 +39.8). 오라클 **+12.36**,
out-of-year `2023→2024` **+5.21** / `2022→2024` +8.31.

**🔴 반증 — 이득은 판별력이 아니라 레벨이다.**
```
① 배포 shift(−0.043768)를 넣으면 사라진다
     shift 없음  Δ +12.36  |  배포 shift 있음  Δ **+0.37**   (021 풀스택 +0.36)
     분리계수도 붕괴:  c그외 +0.0728 / c2S −0.0529  →  +0.0171 / −0.0040
② 기전이 세그먼트별 `d` 적합이다 — (w−mu_w)가 세그먼트 내에서 중심화돼 있지 않다
     E[w−mu | 그외] = −0.113    E[w−mu | 2S] = +0.281    차이 +0.394
     세그먼트별 c = 세그먼트별 **절편**. §6이 금지한 `d` 적합이다
③ out-of-year도 레벨 제거 후 죽는다:  +5.21 → **+1.00** / +8.31 → **−0.07**
④ 이미 측정하고 기각한 축이다 — probe_bias_hetero.json `count_state`
     **2024 = 10.8 / 2023 = 1.4**  (08 §5-15 "이질적 드리프트 보정")
     카운트별 편향 부호도 갈린다: 0-2·1-2·2-2 음수인데 **3-2는 +0.0072**
```

**🔴 절차 실패 3가지 → §0-7·§0-8 신설의 계기가 됐다**
1. `probe_offset_axis.py`에 **레벨/판별력 분리 테스트를 만들어놓고 새 후보에 적용하지 않았다.**
   그 한 줄이면 +12.36 → −3.56이 바로 보였다. → §0-8, 그리고 `test/gate.py` G2.
2. 잔차 지도가 **정규화 아티팩트**였다. → §0-7, `gate.py` G5.
3. 같은 세션에서 읽은 숫자(10.8)를 새 발견(13.18)과 대조하지 않았다.

## 3-Z. 📮 이 노트의 결산 (2026-08-24 갱신)

> 이 노트는 **피처 분석 전용 세션**이 썼다. 제출은 메인 세션이 한다.
> 기준선: **run 015 = LB 1051.73**. 100위 1124.70(−73.0) / 1위 1421.99.

### ✅ 채택돼 점수가 된 것

| 발견 | 결과 |
|---|---|
| **숨은 투구 라벨 8종 복원** (§1-C) `test/recover_labels.py` | → run 009/010 실패모드 offset, **LB 881.73 → 950.96 (+69.3)** |
| **era 보정 asof** (§3-A) → 메인이 `시즌내 성적 분해`로 구현 | → run 013, **로컬 +75.3 / LB +53.7. 단일 피처 축 역대 최대** |
| **`mu` 우연분 상한 9.5 BSS** (§1-K) | 08 §5-3 미해결 질문의 답. offset 이득의 85%가 재현 가능함을 보임 |

### 🔒 구조적으로 닫힌 축 (재시도 금지 — 근거가 "안 해봐서"가 아니다)

| 축 | 닫힌 근거 |
|---|---|
| **offset 라벨** (§2-D) | `y=0 ⟺ (M∪R) ⊎ W` 예외 0건 → **성분이 정확히 2개, 둘 다 사용 중.** 무관 라벨(ball/strike/inplay/구종)은 예외 없이 +0.2~1.4 |
| **offset 형태** (§2-E) | 기준 6항으로 재판정: V0 ≈ O1 ≈ O2a 구별 불가, O3 사망 |
| **trackman** (§2-F) | 매핑 46%→**99%**로 고치고 `rel_side` sd로 재검정 → 실제 성공률 상관 **−0.037** |
| **드리프트 교호작용** (§3-B) | 11개 축 전부 I²≈0, **BSS 상한 합계 3.0** |
| **카운트별 드리프트** (§2-C) | 최근 체제 I²=4%, p=0.41 |
| **구종** (§3-E) | 오라클 160.5 중 **달성가능 10.7(6.7%)** — `P(구종|x)` sd 0.119 vs 라벨 sd 0.460. 그 10.7도 기존 컬럼(믹스·카운트)의 함수. 실측 +0.6 |
| **투수×상황 offset** (§2-G) | 4전 4패(카운트·이닝·2스트라이크·**주자**). 지속성 corr 좌우 0.412 vs 주자 0.106·월 0.049 → 전이가능 ≤6.8 |
| **행 삭제 처방** (§3-F/§3-G) | 세 번 전부 실패(F 전체 −85.4 · 옛 시즌 · aux_clean) |

### 🔴 이 노트에서 가장 값나가는 것 = §0 규칙 8개

숫자는 낡지만 규칙은 안 낡는다. **§0-2·§0-4를 어긴 4번은 전부 틀렸고, 지킨 2번은 맞았다.**
```
§0-2  산포를 신호로 주장하기 전에 (a) 노이즈 바닥과 비교 (b) Brier 기여로 환산
§0-3  채점 범위를 기본값으로 정하지 말 것 — 목표 연도의 체제로 정한다
§0-4  평균에 넣을 관측은 배포 조건과 같은 것만  (→ CLAUDE.md 채택기준 6항이 됨)
§0-1  아이디어를 내면 모델보다 전제를 먼저 확인
§0-5  offset 표 후보는 여지가 아니라 지속성으로 판정  (2026-08-25 신설)
§0-5b 지속성은 **모양+진폭** 둘 다다. corr만 보면 과보정한다  (2026-08-25, §2-O)
§0-6  후처리는 Murphy 분해로 REL/RES를 갈라놓고 볼 것  (2026-08-25 신설)
§0-7  세그먼트 BSS로 손실 위치를 판단하지 말 것 — raw Brier 전역기여로  (2026-08-25)
§0-8  새 후보에는 이미 만든 판정 도구를 전부 돌릴 것  (2026-08-25)
```
⚠️ **근거가 사라진 필터는 같이 사라져야 한다** — §2-E 오류의 진짜 축.
채점을 R만→전체로 바꾸면서 배제 기준(2023 출처 제외)을 안 고쳐 반증을 스스로 가렸다.

### 📦 산출물 (전부 `test/`)

```
gate.py                                          🔥 후보 판정 5종 G1~G5. 새 아이디어는 여기부터
recover_labels.py + recovered_labels.csv.gz      숨은 라벨 8종, 자가검증 assert
map_trackman.py  + pitcher_map_seq.csv           게임시퀀스 정렬 매핑 99%, 716명
build_tm_profile.py + tm_profile_le{2023,2024}   trackman 물리 프로필
probe_offset_forms.py + _preds.csv.gz            4연도×3라벨 예측 캐시 (형태 추가 시 재학습 불필요)
probe_aux_labels.py + probe_aux_val2024.csv.gz   보조 라벨 탐침 + 2024 예측 7종
probe_inplay.py                                  inplay 라벨 추출·검정
probe_situation_headroom.py                      투수×상황 여지·지속성 (§2-G). 학습 0회
probe_gametype_axis.py                           투수×game_type 여지·지속성 + F 해부 (§2-G). 학습 0회
probe_2strike_residual.py                        0-2·1-2 잔차 구조 + 귀무 대조 (§2-K). 학습 0회
probe_sabermetric.py                             인접행 차분 결과복원 + 세이버 지표 (§2-N). 학습 0회
probe_role_table.py + common/role.py             투수 역할 표(등판당 투구수) (§2-P). 학습 0회
probe_pitchtype_ceiling.py                       구종 오라클 160.5 / 달성가능 10.7 (§3-E). 학습 0회
probe_brier_decomp.py                            Murphy 분해 REL/RES + LB 환산 (§1-M). 학습 0회
probe_offset_axis.py                             offset=판별력 축 증명 + b 민감도 (§1-N). 학습 0회
probe_offset_c_split.py                          ❌ c를 2스트라이크로 분리 (§3-J, 반증됨)
probe_breaking_offset.py + _preds.csv.gz         ❌ breaking offset 항 (§2-J). 연도별 보조모델 3개
```

### ⬜ 남은 것

- **O4** 보조모델 피처셋을 메인과 다르게 (§3-I) — 유일하게 안 건드린 축, 기대치 낮음
- CLAUDE.md 다음 방향: 시드 3→7(+5.6) · 보조모델 시즌내 열 · 팀원 `p_matchup`(+3.3)
- ⚠️ 셋을 다 더해도 100위까지 −73을 못 채운다. **새 정보가 아니라 새 문제 정의가 필요한 지점.**
- ❌ ~~**§3-J** offset `c` 2스트라이크 분리~~ — **같은 날 반증.** 이득이 판별력이 아니라
  **레벨**이었다(배포 shift 넣으면 +12.36 → +0.22). 세그먼트별 `d` 적합 = §6 위반.
  **살아있는 후보 없음.**
- 🔴 **2026-08-25 갱신**: 투수×상황(§2-G)까지 닫히며 **offset 표 축도 좌우 하나로 끝났다.**
  잔여 후보를 지속성 기준으로 훑어야 한다면 남은 미측정 칸은 `투수×타자유형`·`투수×구장`
  (⚠️ `투수×game_type`은 2026-08-25에 재고 닫혔다 — §2-G)
  정도인데 전자는 타자 단독·타자×좌우가 **둘 다 0**이라 기대치가 낮고,
  ~~후자는 컬럼이 없다~~ → 🔴 **오기.** 구장 = 홈팀이고 `top_bottom`으로 결정된다.
  2026-08-26에 파생해서 쟀다 — 지속성 corr **+0.0048**로 사망(§2-Q).

### §2-Q ⚖️ 2026-08-26 오전 — 후보 6개 소진 + 문서 오기 1건
전부 **측정 사유** 기각(기법 논쟁 여지 없음). 상세 수치는 `test/closed.tsv` + 각 probe.

```
후보                    측정                                              결과
RMSE 손실               Δraw +4.50 → 레벨 53% 제거 → +2.10, 부호 2/3, t=0.62   ❌
home_win_expectancy·li  상태 6키로 39,324 조합 **전부 단일값**                 ❌ 정보 0
투수×구장               지속성 corr **+0.0048**                                ❌
투수×타자 쌍 이력        쌍당 중앙 **9구**, 커버리지 51.2%, corr +0.05~0.12      ❌
p_matchup(log5)         팀원 LB 실측 **+3.29** (1007.41→1010.70)               ❌
주모델 league baseline   09 §3-B 이론상한 **3.0** ↔ 팀원 LB **+3.33** 일치      ❌
보조모델 baseline        팀원 LB **+1.05**                                      ❌
나무 수 / 정지 기준      레벨보정 정점 350그루 = 현행 278 대비 **+2.4**(sd 5)    ❌
```

**🔴 문서 오기 정정** — 09 §3의 "투수×구장은 컬럼이 없다"는 틀렸다.
`top_bottom=='T'`면 `pitcher_team_id`가 홈 = 구장(정합성 위반 0). 파생된다. 다만 corr 0.005.

**⚠️ WE 프로브 v1의 실수**: `base_state`를 키에서 빼먹어 23.4%가 다값으로 보였다
(주자 1명의 1b/2b/3b 구분). **키 누락 주의.**

**❌ trackman LUPI 확장 불가** — 실패모드 offset(+63.7)은 특권정보 학습이다. 같은 구조를
trackman 연속값으로 넓히려 했으나 30열에 **`plate_x`/`plate_z`/존 좌표가 없다**(릴리스와
무브먼트뿐). 제구의 정답 좌표가 없으니 연속 보조타깃을 못 만든다.
📎 대신 `trackman_game_id`(날짜+구장+경기번호)·`game_date`·`pitch_no`가 있다 — train 표
정교화용으로만 남는다(투수 상수 천장 ~90, 대부분 소진).

**✅ 팀 제출표로 문서 무결성 확인** — 003/004/005/009/010/012/015/017/018/020/021/025/030
열세 개가 우리 기록과 전부 일치. 팀원 파이프라인 천장은 **1024.23**(−32.8)이라 가져올 축이 없다.

**🔥 분포 관찰** — 상위 100팀이 1126~1209의 **83점 폭에 밀집**하고 60위가 **제출 9회**다.
격차 산술: 우리 Brier 0.246807 / 1위 0.246427, 차이 0.00038 = **시즌내분해+실패모드offset+shift를
통째로 하나 더** 가진 크기. 노브로 나올 수 없다.
🔴 밀집 원인으로 "공유된 해법"을 의심했으나 **DACON 코드 공유 탭에는 baseline 외 아무것도
없다**(사용자 확인). 이 가설은 검증 경로가 닫혔다.
⚠️ 규정 원문 재확인(`참고자료/규칙.txt` 2-4): test 행간 통계는 **명시적 금지**. 우리 제약은
자의적이 아니고, 상위 ~100명은 전원 코드 검증 대상이다.

### §2-R 🔥🔥 2026-08-26 — **로컬 부호가 뒤집혔고, 원인이 잡혔다** (run 044, LB 1062.55)

`021 + cond_ph를 트리 피처로` 풀스택 빌드. 039가 로컬로 "offset과 대체재"라 닫은 그 축이다.

```
단계          021 체인                044 체인               Δ
주모델        013  859.0              041  873.1            +14.1
offset 계수   b −0.0990 c 0.0074      b −0.0884 c 0.0109    b 축소
shift         −0.043768 (est .4762)   −0.042452 (est .4752)
platoon b     2.1195                  1.3821                축소
platoon 이득  912.4→932.9  **+20.5**  924.4→930.0  **+5.6**  −14.9
최종 로컬     **932.9**               **930.0**             **−2.9**
LB            1057.00                 **1062.55**           **+5.55**  ← 역전
```

#### 🔴 원인 — 두 경로를 다른 잣대로 쟀다
```
주모델(트리 피처) 이득   ≤2023 학습 → 2024 평가      = out-of-year   정직
platoon/offset 이득      2024 안에서 **투수분할 CV**  = in-year       부풀려짐
```
§7 총평이 *"in-year 교차검증(K-fold·투수분할·split-half)은 지속성 검사를 대신하지 못한다 —
연도를 고정하면 그 해의 노이즈 실현이 구조처럼 보인다"* 고 써놓고, **platoon 이득을 정확히
그 방법으로 쟀다**(021 result.json: "2024 검증 투수분할 CV 912.4 → 932.9").

산술이 맞는다: 트리 +14.1(정직) + platoon 진짜 손실 ≈ −8.5 → **+5.6 ≈ 실측 +5.55.**

🔥 **함의 — "트리 피처 vs offset 표"를 로컬로 비교한 모든 판정이 offset 쪽으로 기울어 있다.**
- §7-2 *"두 경로는 대체재이고 같은 정보를 같은 크기로 나른다. 나머지 축도 결과가 같을 것이다"*
  → **반증됐다.** 로컬 소견이었다.
- 2요인 모형의 *"베이스 1057에서 배율 ≈0"* → 입력(로컬 Δ)이 편향돼 있었으므로 재산정 필요.
- 🔴 004(cond 4표 번들) LB −5.4는 **번들 탓이 맞았다.** ph 단독은 +5.55다.

#### 📎 확인된 진단 패턴 (재사용 가능)
`cond_ph`는 진짜 신호다 — **주모델 ↑ AND 하류 계수 축소**(offset b −0.0990→−0.0884,
platoon b 2.1195→1.3821). 노이즈 열이면 하류가 안 줄어든다.

#### ⚙️ 파이프라인 수정 3건 (cond 풀스택은 이번이 처음이었다)
```
train_offset.py   cond 열이 있으면 무조건 죽는 가드 → meta["use_cond"] 검사로 완화
train_offset.py   cond_*.csv 를 새 run/model 로 복사 (없으면 추론에서 파일 없음)
build_shift.py    engineer() 뒤에 cond 표 미적용 → script.py와 같은 블록 추가 (§9 이중 추론 경로)
train_local.py    build_zip이 role.py를 무조건 실었다 → USE_ROLE일 때만
```

⚠️ §9 함정 재발 — heredoc `<<'PYEOF'` 안의 `\`가 Python까지 오며 하나로 줄고,
삼중따옴표에서 **줄 연속**으로 먹혀 백슬래시+개행이 사라졌다. 매칭 실패 → 패치 미적용.
**해법: 백슬래시를 문자열에 쓰지 말고 줄 단위(splitlines) 조작 + `ast.parse` + 패턴 assert.**

---

### §2-S ❌ cond 표 확장 3종 + M 노브 — **전부 종료.** 그리고 판정자가 바뀌었다 (2026-08-26)

044(cond_ph, LB 1062.55) 이후 같은 형태를 넓혀봤다.

#### 사전 선별 — 지속성 (`probe_cond_persist.py`, `probe_batter_platoon.py`)
```
표                     corr(MINN30)  corr(MINN100)  지속진폭(corr×가중sd)
ph  투수×타자손           +0.3854       +0.4026        **0.0326**   ← 044, LB +5.55
bh  타자×투수손           +0.3657       +0.4563          0.0237
pc  투수×볼카운트         +0.1050       —                0.0109
bc  타자×볼카운트         +0.0592       —                —
pi  투수×이닝             +0.0237       —                0.0024
phc 투수×타자손×카운트     +0.1683       +0.2651         (3way는 무너진다)
bhc 타자×투수손×카운트     +0.1224       +0.1457
```

#### 결과 — 지속성이 **판정자가 아니다**
```
              주모델(out-of-year)   하류 이득    최종 로컬    LB
044 cond_ph      +14.1 ↑            −14.9 ↓      930.0      **+5.55** ✅
048 cond_pc       −3.1 ↓            +1.6  ↑      931.0      미제출
052 cond_bh       −2.1 ↓            +0.1  ―      929.9      **−9.33** ❌
```
🔴 **최종 로컬 합계는 셋을 구별 못 한다(930.0/931.0/929.9). 주모델만 떼면 완전히 갈린다.**
이유: 하류 이득이 **2024 in-year 투수분할 CV**라 부풀려져 있어 합계에서 신호를 지운다(§2-R).
→ **채택 판정은 주모델 델타 부호로.** CLAUDE.md §7-0으로 승격.

⚠️ **`bh`는 지속성을 ph급으로 통과하고도 −9.33이다.** corr 0.366/0.456, 진폭 ph의 73%.
지속성·진폭은 **필요조건이지 충분조건이 아니다.**
📎 도메인 해석: 타깃이 "**투수가 원하는 곳에 던졌나**"다. 타자가 누구든 투수의 *실행*에는
거의 영향이 없다. `bh`의 연도 간 안정성은 상당 부분 **타자 손 자체의 주변 효과**이고
그건 `platoon_advantage`로 이미 모델에 있다. 08 §5-15("타자 축 잔차 여지 0")와 일관.
→ **타자 축 최종 종료.**

#### M(스무딩) 노브 — 죽었다 (`probe_ph_M.py`)
```
ph  M=20 0.0336 | M=50 **0.0326(현행)** | M=120 0.0312 | M=270 0.0291 | M=600 0.0254
bh  M=50 0.0237 | M=80~120 0.0240(최고)
```
🔴 **corr만 보면 M을 키울수록 항상 이긴다** — 수축이 분산을 줄이기 때문이다(M→∞면 corr→1,
표는 전부 평균 = 무의미). 곱(`corr × 가중sd` = 다음 해로 전달되는 실제 크기)을 봐야 한다.
그 값은 M에 평평하고 **현행 M=50이 최적 근처**다. 09 §2-O 함정의 다른 얼굴.

#### 크기 예측은 여전히 불가
052: 로컬 −0.1 → LB **−9.33** (93배). 부호는 맞았다. **부호만 쓴다.**

---

## 4. 📎 미검증 관찰 / 다음에 볼 것

- ✅ ~~`ball+strike`가 1이 아닌 18.7%는 무엇인가~~ → **`inplay`(인플레이/파울)로 추출 완료.**
  라벨은 나왔으나 기여 +1.0~1.4로 기각(§2-D).
- ✅ ~~`middle ∩ reverse` 겹침이 원인인가~~ → **아니다.** 메인 세션 실측:
  `middle↔reverse` 예측 로짓 상관 **0.162**(낮음), 진짜 원인은 `middle↔wayoff` **−0.610** (08 §5-4).
  `mr`로 합친 것은 옳았으나 **이유가 틀렸다**.
- 📎 `asof_batter_*`도 같은 차분으로 복원되는가 — 대칭이면 되겠으나 투수 쪽과 **같은 이벤트**라
  새 정보는 아닐 것. 우선순위 낮음.
- 📎 미매핑 투수 76명(전체 투구의 0.2%) — 게임 매칭 84.1%를 느슨한 정렬로 올리면 채울 수 있으나
  §2-F로 trackman 축이 닫혀 실익 없음.
