# 공통 지침

추가 정보가 있으면 더 나은 답이 가능할 때는 답변 없이 질문만 한다.
한국어는 간결하게, 명사형 어미로. 결론에 충분한 정보가 없으면 모른다고 답한다.

1. **먼저 생각한다** — 가정을 명시. 해석이 갈리면 제시. 더 간단한 길이 있으면 말한다.
2. **최소 코드** — 요청 밖 기능·추상화·유연성·불가능한 예외처리 금지.
3. **수술적 변경** — 인접 코드 개선 금지, 기존 스타일 유지. 내가 만든 고아만 치운다.
4. **검증 가능한 목표** — "고쳐라" → "재현 테스트를 쓰고 통과시켜라".

---

# CLAUDE.md — 10_LGAimers

LG Aimers 9기 + **LG스포츠 해커톤 Phase 2**.
🔴 **이 파일은 "무엇을 할 것인가"만 담는다. 근거·실험기록은 `강의정리/`에 있다.**

| 문서 | 내용 |
|---|---|
| `강의정리/08_Phase2_데이터_및_전략.md` | 실험·제출 기록, 채택 근거 |
| `강의정리/09_피처분석_노트.md` | 라벨 복원, 축 탐색, **§0 운영 규칙** |
| `강의정리/10_KBO_시즌환경_분석.md` | 시즌 환경·base rate |

## 1. 과제·디렉토리

투구 단위 **제구 성공 확률** 예측 (tabular 이진분류). 목표 = 순위.

```
강의자료/   원본 PDF/ipynb (수정 금지)
강의정리/   01~10 md + README(인덱스)
참고자료/   개요·규칙·평가 txt
open/data/  train.csv 1,475,092×49 (2019~24) | test.csv 2025 실제 245,789행
            trackman_history.csv 1,793,078×30 | sample_submission.csv
test/       ─ 파이프라인 (이 순서) ─────────────────────────────────
            train_local.py → train_offset.py → build_shift.py → build_platoon.py
            verify_submit.py            ← 제출 전 필수. import↔zip 대조까지 한다
            ─ 판정 도구 ────────────────────────────────────────
            gate.py                     ← 🔥 후보 판정 5종. 새 아이디어는 여기부터
            probe_*.py                  ← 개별 실험 재현 (16개, 09 §산출물)
            ─ 자산 ────────────────────────────────────────────
            recover_labels.py + recovered_labels.csv.gz   숨은 라벨 8종
            map_trackman.py + pitcher_map_seq.csv         매핑 99% (축은 닫힘)
            common/{script.py, features.py, cond.py, requirements.txt}  ← zip에 그대로
            data/ → open/data (junction)
            runs/<NNN_name>/{model/, result.json, submitNNN.zip}
            artifacts/auxpred/       003 피처(57열) 보조 검증예측
            artifacts/auxpred_ins/   시즌내 분해 성공모델 검증예측 (013 이후)
              🔴 피처를 바꾸면 이 캐시가 낡는다. 에러 없이 조용히 틀린 계수가 나온다
```

**강의정리 규칙**: 강의당 md 1개, 배지 🔥해커톤핵심/⭐중요/📎참고, 상단 압축요약 → 본문 →
적용포인트 + `💬 Q&A 메모`. 새 자료 추가 시 README 갱신.

## 2. 대회 사실

- **지표 = Brier Skill Score** `max(0, 100000×(1 − Brier/[r(1−r)]))`. 순위가 아니라 **확률 정확도**. 상수예측 = 0점.
- **수료선 549.51** 통과 완료. 종료 **2026-09-02 09:59**.
- **Target `control_success`** 1=성공. 실패 = ①한복판 ②크게 벗어남 ③포수 요구 반대. train 평균 0.524.
- ⚠️ **시즌 base rate 하락** 2019 .565 → 2024 .486 → 2025 ~.476. **랜덤 KFold 금지**(3.5배 부풀림), 시간순만.
- **`asof_*` 19개** = 공식·leak-safe·test에도 제공. **시즌 리셋 없는 통산 누적** → 편향 2024 +.025.
  rate는 소수 6자리 반올림(등식 검사에 `1e-9` 금지), **배타적 분할 아님**(겹침 5만건).
- 🔥 **`game_type` 중요도 1위(24.24%)**, `season` 2위(22.05%). F(퓨처스) 11%, **test에도 있다.**
  F 성공률 2022 .709 → 2023 .473 **체제 붕괴**. **F를 빼면 −85.4** → 빼지 말 것.
- ✅ **숨은 투구 라벨 8종 복원됨.** 실패 ⟺ (한복판∪반대) ⊎ 크게벗어남, 예외 0건 → **분해 성분은 정확히 2개, 둘 다 사용 중.**
- **금지**: 현재 투구 이후 정보 · 2025 trackman · **test 내부 행간 통계**(각 행 독립) · 외부 데이터 · 외부 API.
  - ✅ 시즌내 분해가 합법인 이유: 쓰는 것은 **그 행 자신의 `asof` + 학습데이터**뿐. test 다른 행 0개.
  - ⚠️ **LB 반응으로 `logit_shift`를 역산해 더 미는 것 = δ 프로브 = 회색지대.** 안 했고 할 필요도 없다.

## 3. 모델링 하드룰

- **`pitcher_id`/`batter_id` 범주형 금지.** 개인 암기 → 과적합 (CatBoost 780.0→647.3).
- **`season`은 수치형으로만.** 범주형이면 미관측 시즌 붕괴(780→605).
- **CatBoost `cat_features` 반드시 지정**(저cardinality만). 미지정 −114 = 최대 단일 이득.
- **옛 시즌을 버리거나 가중치를 낮추지 말 것.** 삭제도 감가도 손해.
- **calibration 금지.** 로컬 천장 +89인데 LB 실측 −23. 진짜 근거는 **REL ≲ 6 BSS**(§5).
- **depth 6 고정 — 양방향 실측 완료.** d8 **−34.2** / d5 **−10.47**. LB 국소최적이다.
  ⚠️ 로컬 부호는 한쪽만 맞았다: d8 로컬 +7.6(부호 반대) · d5 로컬 −2.8(부호 일치, 3.7배).
- **시드 3 고정.** 3→7이 약한 베이스 +5.6 → 강한 베이스 −0.52.
- **앙상블 이득 = 분산 감소가 전부.** 구조 다양성은 모델 개수를 맞춰 재면 전부 0 이하.
- **offset `a`(스케일)·`d`(절편) 적합 금지.** `a=1·d=0` 고정. 무제약형은 한 해 건너 −210~−638.
- **`mu`·`global_mean`·`logit_shift`·anchor·platoon 표는 학습 때 계산해 zip에 싣는다.**
  test에서 재계산 = 규정 위반.

## 4. 🔥 계측기 — `test/gate.py`

🔴🔴 **게이트는 기각 판정을 내리지 않는다. 값과 기존 관측점만 준다.**
비용이 비대칭이다 — 위양성 = 제출 1회(예산의 1/40) / 위음성 = **그 축을 영영 잃음**.
그리고 **위음성이 큰 축에 몰린다**: 점수를 만든 네 축 중 둘(shift +47.04 · 외삽 제어)이
판별력 축이 아니었다. 자동 기각을 켜면 그 둘을 잃는다. → 09 §0-11.

```
G1 persistence   ≤T−1 표 vs T 표 corr       기존 관측점(좌우 .412 채택 / 나머지 ≤.106) 옆에 놓는다
G3 deploy_delta  021 풀스택에서 Δ            포화를 보려면 여기서 재야 한다
G4 out_of_year   출처/목표별 Δ와 부호        기준선과 함께 잰다. 적합 요소 있으면 필수
G5 contribution  raw Brier 전역기여          세그BSS는 손실 지도가 아니다
null_control     같은 구조를 섞어서 재기      컬럼을 스캔했으면 필수
level_share      이득 중 레벨 몫 (진단)      🔴 기각 근거로 쓰지 말 것
```

**🔴 판정 불가 영역** — depth · 정규화 · 시드 수 등 **미관측 시즌 외삽 제어**는
관측연도(2024)로 원리적 판정이 안 된다(depth 8 = 로컬 +7.6 / LB −34.2).
`G3(..., extrapolation=True)`가 "판정 불가"를 낸다. **이 부류는 제출로만 확인된다.**

**아직 게이트 밖인 것 4개** — 프로브 base 확인(trackman +120.7 → 실모델 −10.3) ·
모델 잔차 vs 피처 수준(스무딩 69 → 2.4) · 시드/모델 개수 일치(grow_policy +24.8 → +0.8) ·
"이미 측정했나" 검색(두 번 실패).

`python gate.py` 로 기존 관측점 재현.

### 게이트 밖 판정 규칙

1. **fold 2024가 주 기준.** 2023 제외(`best_iteration=0` 죽은 fold), 2021·2022는 참고용
   (그 시절 `game_type` F−R 격차 +0.19~0.21이라 점수의 60~75%가 공짜).
2. **합계로 판정 금지. fold별 부호를 볼 것.**
3. **시드 4개 이상 평균 + 비교군 모델 개수 일치.** 안 맞추면 분산감소를 이득으로 오인.
4. 🔥 **여러 후보를 한 측정으로 줄 세웠으면 그 순위는 증거가 아니다.** 최고값은 노이즈만으로도 ~2.9σ 위다.
   ⚠️ "상위권 구성이 일관되다"는 독립 증거가 못 된다 — 측정이 하나면 당연하다.
5. 🔥 **평균 낼 전이는 배포 조건과 같은 것만.** 배포는 항상 **2024 출처 → 2025 목표**.
   ⚠️ 방향 구분: 이상한 연도가 **목표**면 멀쩡하고 **출처**일 때만 깨진다 (2024→2021 +36.8 vs 2021→2024 −39.4).
6. **제출은 반드시 단일 변수.**

7. 🔥 **지속성은 모양+진폭 둘 다.** `corr`는 프로파일을 표준화해 진폭을 버린다 —
   corr +0.824인 축이 전이에서 −45.20이었다(진폭 연도별 10배 변동). 09 §2-O.
8. ⚠️ **귀무 대조 기준을 구분할 것** — 여러 개 스캔이면 `max(null)`, **사전 지목 단일 가설이면 95분위**.
   섞어 쓰면 위음성이 난다. 실제로 §2-M에서 한 번 잘못 닫았다.

⚠️ 이 기준을 한 세션에 세 번 고쳤다. 새 기준으로 뭘 채택하기 전에 **그 기준이 뭘 측정하는지부터** 확인할 것.

## 5. 🔴 로컬 절대값은 LB 순위를 못 매긴다

| 로컬 2024 | LB 2025 | run |
|---|---|---|
| 199.5 | 274.50 | 001 LGBM(선수ID 범주형) |
| 415.6 | 549.51 | 주최측 baseline RF |
| 783.3 | 881.73 | **003** CatBoost+cat_features+FE10, d6, 3시드 |
| 795.8 | 876.37 | 004 = 003 +cond |
| 803.4 | 842.14 | 005 = 004 +depth8 |
| 810.2 | 945.40 | 009 = 003 +실패모드 offset |
| 807.6 | 950.96 | 010 = 009 +성공모델 시드 7 |
| — | 998.00 | 012 = 010 +전역 로짓 이동 전량 |
| **858.6** | — | 013 = 003 +시즌내 성적 분해 4열 (003 대비 **+75.3**) |
| — | 1051.73 | 015 = 013 +offset +이동 |
| — | 1047.04 | 017 = 015에서 보조모델도 61열 재학습 ❌ −4.69 |
| 866.3 | 1016.39 | 018 = 013 +ins 5열 확장 · **raw**(실수 제출) |
| — | 1046.59 | 020 = 018 +offset +이동 ❌ −5.14 |
| — | **1057.00** | **021 = 015 +투수 좌우편차 offset** ← 최고·현 기준선 |
| 857.2 | 1056.48 | 025 = 021 +시드 3→7 ❌ −0.52 |
| 934.3 | 1056.52 | 030 = 021 +mr 보조에 시즌내 2열 ❌ −0.48 |
| 930.1 | 1046.53 | 034 = 021 +depth 6→5 ❌ **−10.47** |
| 922.5 | — | 038 = 021 +depth 6→4 · **제출 안 함**(034가 축을 닫았다) |

**세 축이 현 점수를 만들었다** (근거 08 §5-6·5-10·5-12):
- **시즌내 성적 분해 +53.7** — `(n₁r₁−s₀)/(n₁−n₀)`. 모델이 못 만드는 값(`n₀,s₀`)을 넣어서 이겼다.
- **base rate 이동 보정 +47.04** — 트리가 미관측 시즌을 외삽 못 한다. 남은 여지 +0.12, 축 종료.
- **실패모드 offset +63.7** / **좌우편차 offset +5.27**.

🔴 **베이스 포화 — 2요인 모형** (관측 8개 전부 설명, 예외 0. 09 §1-N):
```
① 새 정보가 아니면          → 베이스 무관하게 음수      (cond, depth 8)
② 새 정보면 배율이 베이스 강도에 단조 감소
   LB 881.73 → 2.40/1.62 · 945.40 → 1.47 · 998.00 → 0.79
      1051.73 → 0.26      · 1057.00 → −0.5 (025·030)
```
같은 축(시즌내 분해)이 빈 베이스 1.62 / 찬 베이스 0.79 — **축의 성질이 아니라 자리가 남았는지다.**
🔴 **현재 베이스(1057)에서는 무엇을 얹어도 배율 ≈ 0.** 025·030이 그대로 나왔다.
⚠️ **out-of-year 델타로 예측 가능한 것은 부호뿐. 크기는 아니다** (관측 배율 0.26~2.40, 9배 폭).
⚠️ **로컬에서 잰 독립성·비대체성이 LB로 전이되지 않는다** — 021은 베이스 4개에서 확인하고도 0.26이었다.

🔥 **격차의 정체 = 전부 해상도(RES).** `Brier = REL − RES + UNC`로 분해하니 **REL ≲ 6 BSS,
RES = REL의 148배**. calibration·클리핑·스케일·shrink는 전부 REL 도구라 **구조적으로 6점을 못 넘는다.**
상수예측 대비: 우리 **+1.06%** / 100위 +1.12% / **1위 +1.42%** → **1위와의 격차는 틀려서가 아니라
못 갈라내서다**(1위가 35% 더 갈라낸다). 이길 길은 **새 판별 정보**뿐. → 09 §1-M.

## 6. 현재 구성 — run 021 (LB 1057.00)

```
성공모델: CatBoost d6, lr .05, cat_features 지정, FE 10개 + 시즌내 분해 4열, 3시드
          ins_{pitcher,batter}_{success_rate,n}  ← build_anchor()의 기준점으로 분해
offset  : logit(p) = logit(p_success) + b·(logit(p_mr)−mu_mr) + c·(logit(p_wayoff)−mu_wayoff)
          보조모델 3시드(009 복사, 003 피처 57열). b=−0.0990, c=+0.0074
shift   : logit_shift = −0.043768   (2025 추정 0.4762. LB 역산 실제값 0.4764 = 오차 0.0002)
platoon : logit(p) += 2.1195 · split[pitcher_id, plat],  M=270, 사용량 가중 평균 0
```
⚠️ **보조모델은 003 피처(57열)** 다. 주모델(61열)과 Pool을 공유하면 CatBoost가 죽는다
→ `offset.aux_feature_cols`에 목록 저장, `prepare()`로 열 부분집합만 다르게 뽑는다.
⚠️ **피처를 바꾸면 검증예측 캐시를 다시 만든다.** 낡으면 offset 계수가 조용히 어긋난다.
✅ 이 구조의 장점 = **메인 재학습 불필요 → 용량 증가 0 + 재보정 없음.**

**순위 (2026-08-25)**
```
1위 1209.31(제출 93) · 5위 1196.94(34) · 10위 1172.08 · 50위 1144.74
60위 1140.75(9) · 100위 1126.67(35) · **우리 1057.00 (310위, 64회)**
100위까지 −69.7 · 1위−100위 폭 82.6 · 컷 상승 +1.97/일 → 종료시 ~1142 예상 = 필요한 것 +85
```
⚠️ **점수는 제출 횟수와 무관하다** (60위 9회 vs 우리 64회).

**다음 순서**
1. ✅ **depth 축 종료** — 034(d5) −10.47로 양방향 패배 확인. 038(d4)은 폐기.
2. ✅ **039 완료 — 번들링이 원인. 축은 안 열린다**(§7-2). 제출 안 함.
3. ⬜ **남은 슬롯 5개, 살아있는 후보 0.** 문서상 닫히지 않은 방향은
   **주모델 교체(패밀리/타깃 재정의)** 하나뿐인데 목표물이 없다(§7-2 RES 분해).
4. 🔴 **남은 실적자 0-2·1-2도 닫혔다**(09 §2-K). 0-2는 귀무 대조 최고(5.74)가
   실제 최고(3.04)보다 커서 **구조 자체가 없고**, 1-2는 초과가 실재하나 후보가
   지속성에서 죽는다. 원인은 기법이 아니라 정보다.

## 7-2. ⚖️ 파라미터화 감사 — **원인은 번들링. 축은 안 열린다** (2026-08-25)

지적: *"5전 5패 축이 전부 같은 형태(평균이동 표)로만 시도됐다."* → **절반 맞았다.**

```
투수×타자손   004  cond 4표 번들을 트리 피처로   LB −5.4
             021  같은 정보를 offset 표로       LB +5.27      ← 형태가 11.7점 갈랐다
```

**run 039 = 013 + `cond_ph` 하나만** 으로 원인을 분리했다 (09 §2-L):
```
013 (기준)        주모델 859.0   platoon b 2.253   얹은 뒤 887.5   이득 +28.49
039 (+cond_ph)    주모델 872.9   platoon b 1.500   얹은 뒤 885.5   이득 +12.60
```
- ✅ **번들링이 원인이 맞다.** 트리는 `ph`를 쓸 수 있다(+13.9). 004는 죽은 표 3개가 희석했다.
- 🔴 **그래도 죽은 축은 안 열린다.** 최종값이 같다(887.5 vs 885.5, offset이 오히려 2점 낫다).
  **두 경로는 대체재이고 같은 정보를 같은 크기로 나른다.** 나머지 축도 결과가 같을 것이다.
- ⚠️ 039의 +13.9는 채택 근거가 아니다 — 004가 로컬 +12.5 / LB −5.4였던 그 자리다. **제출 안 함.**

**RES 적자는 어디에도 집중돼 있지 않다** (09 §2-M, UNC 제거 + 100회 귀무):
```
투수 개인 0.72x  좌우 0.45x  예측10분위 0.39x   ← 무작위보다 덜 퍼짐
투수 역할 3.55x  ← 유일한 초과였으나 leak-safe 대용으론 귀무 범위 안 = 미래정보 아티팩트
```
🔴 **홀대받는 부분집단이 없다 → 분리 모델·샘플 가중치는 목표물이 없다.**
⚠️ 축별 **주변** 집중도만 본 것이다. 셀 단위로만 드러나는 구조는 못 본다.

## 7. 소진 목록 (재시도 금지)

🔴 **새 축을 파기 전에 여기부터 검색할 것.** 이미 측정한 것을 다시 판 적이 두 번 있다.

| 축 | 결과 | 근거 |
|---|---|---|
| 하이퍼파라미터 9종 | ±15 노이즈 | 08 §6 |
| FE 추가 8종 · 범주형 확장 · 시즌 가중치 · row_id | 전멸 (정보 증가 0) | 08 §6 |
| 깊이조합·grow_policy·신경망 앙상블 | 개수 맞추면 +0.8 이하 | 08 §6 |
| **trackman 물리피처** | 17열·매핑 99%로 재검정, 실모델 잔차 **−10.3** | 09 §2-F |
| **calibration / 후처리 전체** | **REL ≲ 6 = 구조적 상한.** split-half isotonic 같은 연도에서도 −19.0 | 09 §1-M, 08 §6-D |
| asof 재중심화 / 스무딩 편향 | −14.9 / 모델 잔차로는 2.4뿐 | 08 §5-16 |
| BrierScore 조기중단 | 결과 완전 동일 (무효) | 08 §6 |
| **구종 축** | 오라클 +160.5인데 **test에서 구종을 모른다**. 달성가능 10.7, 잔여 +2.1 | 09 §3-E |
| **5클래스 다중분류** · `1−s−m−r` 뺄셈 | 용량 5배 / 정보 0 | 08 §6 |
| **최근성 개인기록** | 1시즌 −28.5 · 편차형 −12.6 · 3시즌 부호 갈림. `prev1/3/5`는 이미 시대-청정 | 08 §5-16 |
| **offset 형태 축 전체** | 항 추가 전멸 · `c=0` 제거도 +0.1 · 새 보조 라벨 = 성분 2개뿐(구조적 종료) | 08 §5-4·5-8 |
| **offset 계수 축** | `b` ±20%에 0.6점. 연도 추세 외삽 −0.37 | 09 §1-N |
| **offset `c` 2스트라이크 분리** | 오라클 +12.36이 **배포 shift 넣으면 +0.37**. 세그먼트별 `d` 적합이었다 | 09 §3-J |
| **투수×상황 offset 클래스** | **12전 1승.** 지속성 corr 좌우 **0.412** vs 나머지 전부 **≤0.106**(3개는 음수). 상대 실력축도 죽는다 — 좌우만 **몸의 기하** | 09 §2-G |
| **F/R 축** | F는 **이득원**(raw Brier F<R). 천장 +9.6이고 원인이 이력 부족 → 실현가능 0 | 09 §2-G |
| **타자 축 전체** | `asof_batter_*`가 3열뿐. 가능한 조합 둘 다 시험됨. 잔차 여지 0 | 08 §5-15 |
| 시즌내 분해 확장 2종 | 보조 재학습 −4.69 · 주모델 ins 5열 −5.14 | 08 §5-10 |
| 시드 3→7 · 성분별 EST · mr 보조 시즌내 | −0.52 / 미제출 / −0.48 | 08 §5-14 |
| **시즌내 구종 믹스 앵커** | 018의 5열과 분모·성격이 달라 미시도였다. shift sd .05~.06인데 계수 부호가 뒤집힌다(+.004/−.231/−.129). 더 던지는 건 **먹혀서**다 | 09 §2-L |
| **`asof`가 안 주는 투수 통계** | 기복·0-2프로필·등판부담. ⚠️ shift 없이 재면 여지 40.9·volatility +31.4로 **완전히 속는다**(전부 레벨). shift 넣으면 여지 14.8·절편만 +0.24 | 09 §2-L |
| **세이버 지표**(ERA·WHIP·FIP·K%·BB%) | 인접행 차분으로 복원 성공했으나 **주최측이 이미 줬다** — `asof_strike_rate`↔K% **+0.898**, `asof_ball_rate`↔BB% **+0.908**. 잔차 7개 전부 귀무 이하 | 09 §2-N |
| `asof_pitcher_pitchmix_n` | **`asof_pitcher_n`과 100.0000% 동일한 중복 열.** 짝 어긋남 자체가 없다 | 09 §2-N |
| **투수 역할 표**(등판당 투구수) | train 정적 표라 표 자체는 안정(corr +0.741). 그런데 레벨 보정 전이 **−8.08**(진폭 부호 1/3), 트리 피처 **−11.0**(결측 행이 더 나빠짐 = 분할 오염) | 09 §2-P |
| **월당 소화량**(`ins_pitcher_n`/경과개월) | 지속성 corr **+0.824**(좌우 0.412의 2배)로 통과했으나 **배포 전이 −45.20**. 진폭이 해마다 10배 흔들려 과보정 | 09 §2-O |
| PCA/피처 압축 | 비지도(η²=0.00025라 성분에 안 남음) · 축 회전이 트리 분할을 깸 · `cat_features` 못 태움 | 09 §2-N |
| prior-shift 보정(Saerens EM) · 포수 프레이밍 | 규정 위반 / 포수 컬럼 없음 | 08 §5-15 |

🔥 **2026-08-25 총평 — 병목은 "정보원이 없다"가 아니라 "해를 넘기지 못한다"** (09 §2-L)
이날 후보 7개가 전부 죽었는데 **죽은 지점이 한 곳**이다:
```
c 2S분리      in-year +12.36 → 레벨제거 −3.56      p_breaking  in-year +5.2  → out-of-year −20.62
투수 기복      in-year  +5.10 → out-of-year −26.02   구종믹스     자기적합 +4.7 → −0.05~+0.92
투수×주자 여지 77 → 지속성 0.106     투수×월 여지 93 → 0.049     013×018 블렌드 상관 0.99471
```
🔴 **in-year 교차검증(K-fold·투수분할·split-half)은 지속성 검사를 대신하지 못한다** —
연도를 고정하면 그 해의 노이즈 실현이 구조처럼 보인다. **반드시 연도를 건너서 잰다.**
**연도 간 지속성을 통과한 것은 좌우편차(+0.3125) 하나뿐**이고 그게 021이다.
⚠️ 죽은 fold 2023 자기적합이 이날만 두 번 튀었다(+60.93 · +104.95). **배포 최근접 전이만 본다**가 매번 막았다.

## 7-1. 🔴 판별식 — 새 축을 볼 때 이것부터

**① 정보가 느는가  ② 행마다 변하는가  ③ 해마다 이어지는가**

소진 목록은 대부분 ①에서 죽었다 — **기존 컬럼 재조합 = 정보 증가 0**. 시즌내 분해(+75.3)만
모델이 도달 불가능한 값을 넣어서 이겼다. **표현력을 늘리는 방향은 재시도 금지.**

- ②의 근거: 투수 **상수** 피처의 천장은 **~90 BSS**다(전역기여 환산: asof_n ~300 12.6 / ~1.5k **48.2** / ~5k 19.0 / ~15k 10.2).
  🔴 옛 기록의 "~13"은 **계산 오류**다 — `probe_pitcher_headroom.py:43-44`가 잔차는 투구수 가중,
  노이즈는 투수 단순평균으로 계산해 과다 차감했다(참신호 0인 가짜 데이터: 가중 +4~7 ≈ 0 vs 단순 −50).
  같은 규약을 쓴 08 §5-12 표도 과소평가다(투수 단독 22→**44**, 투수×좌우 126→**209**).
  ✅ **그래도 클래스는 닫혀 있다 — 근거가 "천장이 낮다"에서 "정보원이 없다"로 바뀔 뿐.**
  trackman 17열 → 013 잔차 **−10.3** · `rel_side`→성공률 −0.037 · **trackman→좌우편차 OOF corr −0.12/+0.05**
  (마지막 것이 "투수 상수 × 상황 교호작용" 가설의 최유망 사례였고 음성. 021 기준선은 +0.3125).
  → `test/probe_pitcher_ceiling_fix.py`. **새 투수 상수 정보원이 생기면 최대 90까지 열려 있다.**
- ③의 근거: 행마다 변하기만 해선 부족하다. 투수×주자·투수×월은 ②를 통과하고 ③에서 죽었다.
  **판정 도구는 여지가 아니라 지속성**(= `gate.G1`).
- ⚠️ **정보가 늘어도 서로 겹치면 진다** — 020의 ins 5열이 그랬다(전부 "올해 환경"을 인코딩).
- 🔴 **정보원 축은 전부 닫혔다**(trackman 포함). 남은 것은 정밀도·분산감소뿐인데 그것도 포화다.

## 8. 제출 규칙·워크플로우

- `submit.zip` = **최상위에 `model/` + `script.py` + `requirements.txt`**. 추가 최상위 폴더 = 설치오류.
- script.py는 `./data/test.csv`·`sample_submission.csv` 읽고 → `./output/submission.csv`.
  `./data`는 읽기전용, 서버가 실제 test로 교체.
- 제한: 추론 ≤10분(24.6만행) · 설치 ≤10분 · zip ≤10GB · **1일 5회(팀 공유)**.
  오류 2종: 설치오류(차감 X) / **제출오류=script 실행실패(차감 O)**.
- 서버: Ubuntu22, Python **3.11**, L4 22.4GB, 6vCPU/28GB, **오프라인**.
  기본설치에 lightgbm/xgboost/catboost 없음 → requirements.txt에 명시.

1. `train_local.py` 상단 `RUN` 변경 후 실행 — 기존 RUN 있으면 **실행 거부**(덮어쓰기 방지)
2. **`verify_submit.py <zip>` 전체규모 검증 필수** — 245,789행 가짜 test로 구조·시간·결측·범위
   🔴 **2026-08-25 보강**: 가짜 test의 `asof_*_n`을 2024 총 투구수만큼 올린다. 안 그러면
   `dn = max(n1−n0, 0) = 0`이라 **시즌내 분해(최대 축 +53.7)가 100% 폴백**으로 떨어진다 —
   015 이후 모든 제출이 그 경로를 미검증으로 내보냈다. 이제 `dn>0` 비율을 리포트하고
   50% 미만이면 실패 처리한다. ⚠️ 대신 **예측 평균은 base rate 검사로 못 쓴다**(0.51로 뜬다).
3. DACON 업로드 → `result.json`의 `lb_2025`에 손으로 기록

- zip은 **Python `zipfile`**로 생성. `Compress-Archive`는 백슬래시 경로라 Linux에서 깨진다.
- 모델은 **네이티브 포맷**(`.cbm`), **pickle 금지**(로컬 3.14 vs 서버 3.11).
- **FE는 `common/features.py` 한 곳에만.** zip에서 빠지면 `ModuleNotFoundError` = 제출 1회 차감.

## 9. 이 머신 함정

로컬 Python **3.14**: pandas3.0.5, numpy2.5.1, sklearn1.9.0, lightgbm4.7.0, **catboost1.2.10**, torch2.13.0+cpu, pypdf.

| 증상 | 원인 | 해결 |
|---|---|---|
| 즉시 종료, 로그 빔 | Windows stdout cp949에 유니코드 | `sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')` — **파일당 1회만** |
| 로그가 끝까지 안 보임 | UTF-8 래퍼 블록 버퍼링 | `print(..., flush=True)` |
| 종료코드 `-1073741676` | CatBoost `thread_count=0` | **CatBoost는 `-1`** (LightGBM만 0=전체) |
| 학습 4배 느려짐 | 6코어에 동시 실행 | 무거운 학습은 순차로 (431s→1819s 실측) |
| PDF 안 읽힘 | poppler 미설치 | `pypdf`로 추출 → scratchpad txt → Read |
| 추론이 `KeyError: 's0_*'` | run마다 `ANCHOR_SPECS`가 달라 `anchor.csv` 스키마 불일치 | 그 run 구성으로 되돌리고 재생성 — **값이 같은지 assert** |
| 측정이 조용히 틀린 베이스에서 돎 | `artifacts/auxpred_ins`가 013이 아니라 **018** 캐시 · `platoon.csv`는 2024 포함(자기적합 +77) | 재구성 직후 `assert abs(got − result.json 기록) < 1` (gate.py에 있음) |
| CatBoost `At position N should be feature ...` | `script.py`와 `build_shift.py`가 추론 경로를 **중복 구현** | 한 곳만 고치면 안 된다. 둘 다 |
| `git add`가 `No such file or directory`(파일은 있음) | 경로에 Windows 예약 장치명 `aux` `con` `nul` `com1~9` 등 | 이름 변경. Python·`cp`·`ls`는 통과하고 git만 막힌다 |

- Bash 워드분할: 한글/공백 경로 loop 깨짐 → python glob.
- ⚠️ **스크립트로 문자열 치환했으면 반드시 검증하고 출력할 것.** 치환은 조용히 실패한다.
  ```python
  ast.parse(src)                     # 문법
  for pat in [...]: assert pat in src  # 각 치환이 적용됐는지
  ```
